# Study Hub — Project Notes (Living Document)

> **What this file is:** a continuously-updated record of *what* we are building, *why* each piece exists, and *what has been done so far*. Anyone — including future-you reading this months later, or an SMU interviewer — should be able to read this top-to-bottom and understand the reasoning behind every decision.

---

## 1. The product, in one sentence

**A research assistant that helps evidence-curious lifters find, understand, critically interpret, and apply exercise-science research — with a secondary system for writing and organizing your own evidence-backed conclusions.**

It is **not**:

- "PubMed but prettier"
- "AI fact-checks fitness influencers"
- "Obsidian for exercise science"
- a social network

It is a **research interpretation layer** between academic literature and a normal lifter.

---

## 2. Why this project exists

- **Personal motivation:** the builder (Adam) reads exercise-science studies and constantly hits the same wall — PubMed contains the information, but it is not designed around the questions a lifter actually asks ("Should I train this muscle 1× or 2× per week?", "Does arm position change which biceps head gets trained?", etc.).
- **Portfolio goal:** this is the centerpiece project for an SMU **Information Systems** application. It demonstrates information retrieval, API integration, relational data modeling, AI/NLP integration, information architecture, and user-centered product design — the exact mix IS values.
- **Scope reality:** built in ~4–6 weeks before an internship in Japan, so strict scope control has been a non-negotiable from day one.

---

## 3. The product hierarchy (the single most important rule)

From the project brief, the hierarchy is:

1. **🥇 Core — Study Explorer (≈70% of the product):** search → ranked results → study page that answers *what was asked / who was studied / how it was conducted / what was measured / what was found / what the authors concluded / limitations / evidence context / what this might mean for training*.
2. **🥈 Secondary — Evidence Notebook:** save studies, take notes, write articles that **consume studies already discovered through the Explorer**.
3. **🥉 Advanced — Evidence relationships:** claims linked to studies (`supports / contradicts / mixed / contextual`), and eventually a visual evidence graph.

**Two hard rules that follow from this:**

- **Explorer-first:** articles are written *after* reading studies. Never build the UI around "write a claim, then have the AI find a study that agrees" — that risks becoming an AI confirmation-bias machine.
- **Rank, don't filter:** search results are *ranked* by relevance but never hidden. A "peripheral" study may still contain something useful; the user always retains access.

---

## 4. Core product principles (the "defensible" rules)

These exist because the original idea drifted toward "AI tells you if a study is reliable," which is scientifically indefensible and easy to dismantle in an interview:

1. **No numerical "credibility score."** No "🟢 87% reliable." Study quality is not a scalar. Instead: **Evidence Context** — surface the factors (sample size, study design, population, training status, duration, measurement) with plain-language explanations of *why each matters*.
2. **The AI is an interpreter, never the source.** The raw PubMed abstract is always displayed first, unmodified. The AI only explains/extracts *downstream* of the source record.
3. **Three distinct AI jobs, kept structurally separate:**
   - **Job 1 — Extract:**  structured facts (sample size, population, duration…) — relatively objective.
   - **Job 2 — Explain:**  plain-English understanding of what the study tested/found.
   - **Job 3 — Translate cautiously:** what this *might* mean for training — explicitly labelled as interpretation, with "what it does NOT mean".
4. **Don't let AI invent limitations.** "n=24 trained males, 8 weeks" → AI can extract that. "This sample is too small to be reliable" → that is AI *interpretation* and must be labelled as such. "Ultrasound was inadequate" → scientific judgment, not allowed in the extraction layer.
5. **Immutable source, regenerable AI:** the PubMed record is never overwritten. AI output lives in separate tables so a prompt/model change can regenerate interpretations without touching the source.

---

## 5. Architecture & tech stack (and why)

```
                    ┌──────────────┐
                    │    User      │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │   Next.js    │
                    │  React/TS    │
                    └──────┬───────┘
                           │
             ┌─────────────┼──────────────┐
             ▼             ▼              ▼
        ┌─────────┐   ┌─────────┐   ┌──────────┐
        │ Supabase│   │ PubMed  │   │ DeepSeek │
        │ Postgres│   │  E-util │   │   API    │
        └─────────┘   └─────────┘   └──────────┘
        · Auth (GitHub OAuth, Task 6)
        · RLS
        · Storage (future)
```

| Piece | Choice | Why |
|---|---|---|
| Frontend + Backend | **Next.js 16 (App Router) + TypeScript** | Same language ecosystem as the Arctic Fever React Native work; one repo for frontend + API routes; one deploy. No context-switching. |
| Database | **PostgreSQL via Supabase** | Relational fit for the evidence graph; Supabase gives auth/storage/RLS without setup overhead; free tier. |
| Auth | **Supabase GitHub OAuth** | First user-owned feature (Task 6) needs real accounts. OAuth beats hardcoded test users (unprofessional for a portfolio) and email magic links (SMTP/spam-filter headaches). GitHub OAuth ties onto the GitHub account holding the student developer benefits; ~2 minutes to configure in the dashboards. |
| External research data | **NCBI PubMed E-utilities** | The authoritative open API for biomedical literature. Two-step: `esearch` → PMIDs, then `efetch` → full XML. |
| AI | **DeepSeek API** | Cheap structured extraction; model chosen explicitly in code (see §8). |
| Graph physics | **d3-force** | The task explicitly required using d3-force for the force-directed layout math (forceLink / forceManyBody / forceCollide / forceCenter). Rendering stays a self-contained SVG (`src/app/graph/EvidenceGraph.tsx`). |
| Hosting | **Vercel + Supabase** (planned) | Natural fits for Next.js + Postgres. |

### Why Next.js 16 matters for the code
This is a **new major version** with breaking conventions vs. older Next.js training data. The key ones we've hit: **dynamic route `params` is now a `Promise`** and must be `await`-ed (see `src/app/study/[pmid]/page.tsx`); `cookies()` is async-only; `middleware` is renamed `proxy`. Always check `node_modules/next/dist/docs/` before using a new Next.js feature.

---

## 6. The database design (the data-model reasoning)

The single most important schema decision: **separate the source of truth from derived data.**

```
STUDIES              = "what the paper/source says"   (immutable, from PubMed)
  ▼
STUDY_CONTEXT        = "what our AI extracted"        (regenerable)
  ▼
STUDY_ASSESSMENTS    = "how that context affects interpretation" (qualitative)
```

And separately, the user's evidence graph:

```
ARTICLES → CLAIMS → EVIDENCE_LINKS → STUDIES
```

And the user's private layer (Task 6):

```
STUDY_NOTES (user_id, study_id)    = one consolidated personal note per (user, study)
```

Why this separation matters:
- If the AI prompt improves next month, you can **wipe and regenerate** `study_context` without destroying the original PubMed record.
- It keeps "source facts" vs "AI interpretation" vs "what the user concludes" cleanly separated — which is exactly the defensible design the project brief demands.

Tables (all in `sql/schema.sql`, which is the source of truth):

| Table | Purpose |
|---|---|
| `users` | Profile extension of Supabase Auth |
| `studies` | Raw PubMed record (PMID-unique) |
| `study_context` | AI-extracted structured facts (1:1 with study), incl. `source_info` (what the AI read) |
| `study_identified_limitations` | AI-derived limitations child rows (`limitation`, `based_on`, `sort_order`), regenerated wholesale |
| `study_simplifications` | AI plain-English re-explanation of the study (1:1 with study, Job 2), regenerable like `study_context` |
| `study_notes` | User-owned personal note per (user, study) — `UNIQUE(user_id, study_id)`, RLS-locked to `auth.uid()` |
| `study_assessments` | Qualitative evidence profile (1:1 with study) |
| `articles` | User-written wiki-style conclusions |
| `claims` | Individual statements within an article |
| `evidence_links` | The graph: claim ↔ study with a `relationship` enum |

---

## 7. Security decisions (RLS — Row Level Security)

Supabase's RLS is a per-table bouncer. Our decisions:

- **`studies` = shared public library** (like Wikipedia entries): anyone can **READ** and **INSERT**, **nobody can UPDATE or DELETE** via the API.
  - Rationale: the raw PubMed record is source-derived. Arbitrary public users must not be able to modify existing cached studies.
  - **Why INSERT-only save?** Originally the endpoint used PostgreSQL `upsert`, which requires UPDATE permission. A security review flagged that granting public UPDATE contradicts the "immutable source" principle. `/api/save-study` now does **check-then-insert**; `/api/save-context` inserts the `studies` row the same way if missing.
- **`study_context` = shared regenerable derived library** (DIFFERENT trust model than `studies`): AI-derived output is *designed* to be overwritten — a prompt/model change can regenerate it. So RLS is **SELECT + INSERT + UPDATE** (upsert on `study_id`), with DELETE locked (a full-row upsert replaces content wholesale).
- **`study_identified_limitations` = regenerable child**: regeneration = delete-all + reinsert, so RLS is **SELECT + INSERT + DELETE** (no UPDATE — rows are replaced wholesale).
- **`study_simplifications` = shared regenerable derived library** (Task 9, Job 2): identical trust model to `study_context` — **SELECT + INSERT + UPDATE** (upsert on `study_id` = regenerate), DELETE locked. `/api/save-simplification` mirrors `/api/save-context` exactly (check-then-insert the `studies` row if missing, then upsert the simplification).
- **`study_assessments` = shared regenerable evidence profile** (Task 10, Job 3): identical trust model to `study_context`/`study_simplifications` — **SELECT + INSERT + UPDATE** (upsert on `study_id` = regenerate), DELETE locked. `/api/save-assessment` mirrors `/api/save-context` exactly. The Task 10 schema recreates the table (was flat placeholder + no INSERT policy; zero rows existed) with the evidence-context + training-application columns.
- **`study_notes` = PRIVATE user-owned notes** (Task 6, the first user-owned table): a single `FOR ALL TO authenticated` policy with `USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)` — a user can only see/edit their OWN rows, and cannot insert on another user's behalf. The `user_id` column **DEFAULTS to `auth.uid()`** in the DB, so the client never sends it (belt-and-suspenders on top of the WITH CHECK).
- **`articles`/`claims`/`evidence_links` = PRIVATE user-owned evidence graph** (Task 7): a single `FOR ALL TO authenticated` policy per table with `USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)` — same trust model as `study_notes`; `user_id` **DEFAULTS to `auth.uid()`** so the client never sends it. Explicit `GRANT ALL TO authenticated` + `REVOKE ALL FROM anon` make the permissions visible. `evidence_links` gets a surrogate `id` PK + `UNIQUE (claim_id, study_id)` so the editor can update/delete one link by id. `users` remains RLS-enabled with **no policies**.
- **RLS indexes (performance):** every `user_id` / FK column used by the policies and the per-study lookups is indexed (`idx_articles_user_id`, `idx_claims_article_id`, `idx_claims_user_id`, `idx_evidence_links_claim_id`, `idx_evidence_links_study_id`, `idx_evidence_links_user_id`) per Supabase RLS performance guidance.
- **Keys:** only the Supabase **anon/public** key is exposed via `NEXT_PUBLIC_` (it's meant to be public — RLS is the real security boundary). The DeepSeek key goes into `.env.local` **without** `NEXT_PUBLIC_` so it stays server-side. `.env*` is git-ignored (except `.env.example`, the committed template).

---

## 8. Cost model (important — two separate costs)

There are **two completely different costs**, easy to confuse:

| | Cline (the coding agent) | Study Hub (the app's AI features) |
|---|---|---|
| What it is | Helps **write** the code | Runs **inside** the deployed app |
| Cost trigger | Large agentic context (reading files, reasoning, retries) | One small structured API call per study |
| Model choice | Set in the Cline provider config | **Chosen explicitly in our code** — the API key alone does **not** pick the model |

You saw a 43¢ bill from Cline — that was agentic coding cost (lots of context in/out), **not** the app's per-study cost. Study Hub's extraction will be one short call per study.

**Model strategy (in effect now):**
- **Cheap model (`deepseek-chat`)** → ALL FIVE AI jobs: extraction, simplification (plain-English explanation), qualitative evidence profile (Job 3, Task 10), claim↔evidence alignment, AND Job 4 query translation for AI-assisted search (Task 15). Set as the default (via `getModel()` in `src/lib/ai.ts`, which resolves `DEEPSEEK_MODEL` with a `deepseek-chat` fallback — every job shares it; there is deliberately **no** separate heavier-model logic per Task 9 note "do not build logic for a separate heavier model").
- DeepSeek key: `DEEPSEEK_API_KEY=` in `.env.local` (no `NEXT_PUBLIC_` prefix).

---

## 9. What has been built so far

All of this is **committed and pushed** to GitHub (`main`).

| File | What it does | Why it exists |
|---|---|---|
| `src/lib/pubmed.ts` | Shared PubMed engine: `esearch` → `efetch` → XML parse → structured `PubMedStudy` (`pmid`, `title`, `authors`, `journal`, `publicationDate`, `abstract`, `doi`, `pmcid`) + `fetchFullText` (PMC, 12k cap); `searchPubMed` uses `sort=relevance` (NCBI Best Match) | One source of truth for parsing; `findPmcid` reads the PMC ArticleId from the record itself (lag-free); Best Match ranks results by relevance without hiding any (Task 5) |
| `src/app/api/search-pubmed/route.ts` | API route exposing search (delegates to shared lib) | Backend keeps the NCBI call server-side |
| `src/app/api/save-study/route.ts` | POST: check-then-insert a study into `studies` | INSERT-only per security review (§7); no public UPDATE |
| `src/app/api/extract-context/route.ts` | POST: fetch PubMed + PMC full text when available, run DeepSeek extraction, return `{ study, context, sourceInfo }` | The AI pipeline (no DB write) |
| `src/app/api/save-context/route.ts` | POST: persist validated `StudyContext` into `study_context` (+ `study_identified_limitations` child rows) | Wires the "Generate/Regenerate context" persistence; regenerable-warehouse RLS |
| `src/app/api/simplify-study/route.ts` | POST `/api/simplify-study`: fetch PubMed + PMC full text when available, run DeepSeek plain-English simplification (Job 2), return `{ study, simplification, sourceInfo }` | Mirrors `/api/extract-context` for the "In plain English" block; no DB write |
| `src/app/api/save-simplification/route.ts` | POST: persist validated `StudySimplification` into `study_simplifications` (check-then-insert the `studies` row if missing, upsert on `study_id`) | Wires the "Generate/Regenerate explanation" persistence; same regenerable RLS as `study_context` |
| `src/app/api/assess-claim/route.ts` | POST `/api/assess-claim`: resolve linked `studies` abstracts (+ `study_context.findings` when available) and run the DeepSeek claim-alignment check → `{ verdict: aligned | partially_aligned | unaligned, reasoning }` | The "no confirmation-bias machine" guard for the article editor (Task 9) |
| `src/app/study/[pmid]/page.tsx` | Server page: DB-first (loads `studies` + saved `study_context`/limitations + `study_simplifications` + `study_assessments`), falls back to live PubMed; passes `studyId` to `<StudyDetail>`; `force-dynamic` so save-state stays fresh | Saved context/simplification/assessment render without an AI call on revisit; `studyId` unlocks PersonalNotes; Next.js 16 `params`-as-`Promise` |
| `src/app/study/[pmid]/StudyDetail.tsx` | Study detail UI: raw abstract on top → **"In plain English" block (Job 2, regenerable)** → AI Study breakdown → **"Evidence context" (Job 3, six factor cards, no credibility score, regenerable)** → **"What this might mean for training" (Job 3, labelled interpretation + "what this does NOT mean" cautions, regenerable)** → **Personal notes**; header now includes the `AuthStatus` sign-in control; Save-to-Library triggers `router.refresh()` so notes unlock immediately | Encodes the product rules end-to-end |
| `src/app/study/[pmid]/PersonalNotes.tsx` | Personal notes editor (Task 6): login call-to-action when unauthenticated; "save the study first" hint when signed in but unsaved; textarea + INSERT-or-UPDATE against `study_notes` when signed in + saved | First user-owned feature; client never sends `user_id` (DB defaults to `auth.uid()`) |
| `src/components/AuthStatus.tsx` | Header sign-in / sign-out widget (GitHub OAuth), thread-safe `next` redirect back to the originating page | Global auth entry point via `@supabase/ssr` browser client |
| `src/app/auth/callback/route.ts` | `GET /auth/callback`: exchanges the OAuth code for a session via a server-side `@supabase/ssr` client and persists session cookies, then redirects to `next` | Required Supabase PKCE callback handler |
| `src/lib/supabase/browser.ts` | Browser Supabase client (`createBrowserClient`) with automatic cookie session handling | Auth + user-owned notes from the client |
| `src/lib/supabase/server.ts` | Server Supabase client (`createServerClient`) bound to request/response cookies | Used by `/auth/callback` for the code exchange |
| `src/app/page.tsx` | Search home page; cards link to `/study/[pmid]`; header now has `AuthStatus` + Library link | The entry point of the Explorer |
| `src/app/library/page.tsx` | Library page (`/library`): server component listing saved studies from `studies` newest-first with the home-page card markup, empty + error states, and a "Back to search" link; header has `AuthStatus` | Task 4; public shared-library view (`studies` = shared reference library) |
| `sql/schema.sql` | Full schema + RLS as a checked-in file (incl. `source_info` col + `study_identified_limitations` table + regenerable RLS + **`study_notes` table + per-user RLS + FK indexes** + **Task 7: `articles`/`claims`/`evidence_links` upgraded to `user_id DEFAULT auth.uid()`, own RLS `FOR ALL TO authenticated`, explicit GRANTs, FK/RLS indexes, migration section** + **Task 9: `study_simplifications` table + regenerable RLS** + **Task 10: `study_assessments` recreated with evidence-context + training-application columns and SELECT+INSERT+UPDATE regenerable RLS**) | Database reproducible; portfolio artifact |
| `src/lib/articles.ts` | Shared Task 7/8 domain types: `EvidenceRelationship` (supports/contradicts/mixed/contextual) + label/color maps (`RELATIONSHIP_LABELS`/`RELATIONSHIP_COLORS`/`RELATIONSHIP_HEX` for SVG), `LinkableStudy`, `DraftLink`, `DraftClaim`, `ArticleDraft` | Single source of truth for the evidence-graph UI (badges AND SVG edges) |
| `src/app/articles/page.tsx` | `/articles` server shell (header + `AuthStatus`) rendering `<ArticlesList>` | Task 7; entry point to the Evidence Notebook |
| `src/app/articles/ArticlesList.tsx` | Client list of the signed-in user's own articles (RLS-locked) with "New article" (INSERT then navigate to editor) | Task 7; overview + create |
| `src/app/articles/[id]/page.tsx` | `/articles/[id]` server shell passing the id to `<ArticleEditor>` (Next.js 16 `params`-as-`Promise`) | Task 7; dynamic route |
| `src/app/articles/[id]/ArticleEditor.tsx` | Full article editor: title + content textareas, add/remove/edit claims, per-claim study picker (searches saved `studies`) + relationship dropdown, diff-save (INSERT/UPDATE/DELETE across articles/claims/evidence_links), second-save safe (real IDs patched back into the draft); **Task 9 per-claim "Check alignment" button calling `/api/assess-claim` → verdict chip (aligned/partially aligned/unaligned) + reasoning (cleared when claim text or links change)** | Task 7; the core UI — Task 9 adds the alignment guard |
| `src/components/StudyReferences.tsx` | Study-page widget: lists the signed-in user's claims that reference this study (relationship badge + article link); login/save hints otherwise | Task 7; closes the loop — the study page shows which claims cite it |
| `src/app/graph/page.tsx` | `/graph` server shell (header + `AuthStatus` + nav) rendering `<EvidenceGraph>`; `force-dynamic` | Task 8; entry point to the visual evidence graph |
| `src/app/graph/EvidenceGraph.tsx` | Task 8 client component: loads the user's ARTICLES → CLAIMS → EVIDENCE_LINKS → STUDIES via the browser Supabase client (RLS-locked), builds a graph data structure, runs **d3-force** (`forceLink`/`forceManyBody`/`forceCollide`/`forceCenter`) over it, and renders a self-contained SVG (relationship-colored edges via `RELATIONSHIP_HEX`, arrowheads on evidence edges, legend, node counts). Clicking a node navigates to the article editor (`/articles/[id]`) or study page (`/study/[pmid]`) | Task 8; the flagship portfolio "wow" — a real, interactive evidence graph |
| `src/lib/ai.ts` | Server-only DeepSeek helper: `extractStudyContext(title, abstract, fullText?)` → validated `StudyContext` + `IdentifiedLimitation[]`; **`simplifyStudy(title, abstract, fullText?)` → `StudySimplification` (Job 2); `assessStudy(title, abstract, fullText?)` → `StudyAssessment` (Job 3, Task 10: evidence-context factors + "what this might mean for training" + "does NOT mean" cautions); `assessClaimAlignment({claimText, studies})` → `ClaimAlignment` (verdict + reasoning); shared `getModel()`/`callDeepSeek()` — ONE cheap model (`deepseek-chat` default) for ALL jobs, NO heavier-model logic** | `deepseek-chat` default; strict JSON validation; two-tier limitations; no credibility score |
| `src/app/api/assess-study/route.ts` | POST `/api/assess-study`: fetch PubMed + PMC full text when available, run DeepSeek qualitative assessment (Job 3), return `{ study, assessment, sourceInfo }` | Mirrors `/api/extract-context` / `/api/simplify-study` for the "Evidence context" + "What this might mean for training" blocks; no DB write |
| `src/app/api/save-assessment/route.ts` | POST: persist validated `StudyAssessment` into `study_assessments` (check-then-insert the `studies` row if missing, upsert on `study_id`) | Wires the "Generate/Regenerate assessment" persistence; same regenerable RLS as `study_context`/`study_simplifications` |
| `.env.example` | Committed template of required env vars (no secrets) + GitHub OAuth note | `.env*` stays git-ignored except this file |

### The page layout that encodes the product rules (`StudyDetail.tsx`, top to bottom)

1. Header (title, authors, journal, PMID) + `AuthStatus` sign-in/sign-out control
2. **Save to Library** + **View original on PubMed** buttons
3. **What the study actually says** — the raw abstract, unmodified (source first)
4. **In plain English** (Task 9, Job 2) — AI plain-English re-explanation for a curious lifter; `sourceInfo` badge, Generate/Regenerate button, loading skeleton, error + save-failure states; persisted to `study_simplifications` (DB-first on revisit)
5. **Study breakdown** — AI-generated: research question / who was studied / how conducted / findings / paper-stated limitations + identified limitations (each with a `based_on` quote), `sourceInfo` badge, Generate/Regenerate button, loading skeleton, error states
6. **Evidence context** (Task 10, Job 3) — six factor cards (design, sample size, population, training status, duration, measurement), each with plain-language "why it matters" tied to THIS study; `sourceInfo` badge, Generate/Regenerate button, loading skeleton, error + save-failure states; explicitly no credibility score; persisted to `study_assessments` (DB-first on revisit)
7. **What this might mean for training** (Task 10, Job 3) — clearly labelled "Interpretation — not established fact" badge; "If this generalises, it might suggest..." + "What this does NOT mean" cautions; same regenerable persistence as Evidence context
8. **References in your articles** (Task 7) — the signed-in user's claims referencing this study, with relationship badge + link back to the article; hint to save the study first when unsaved
9. **Personal notes** (Task 6) — login-gated textarea saving to `study_notes`; private to the signed-in user

---

## 10. Current status

- ✅ Next.js 16.3.1 + TypeScript + Tailwind, running with `npm run dev` (port 3000)
- ✅ Supabase project connected; `.env.local` git-ignored
- ✅ Full schema created in Supabase; recorded in `sql/schema.sql`
- ✅ PubMed search → parse → study card UI
- ✅ Study detail page (DB-first, live fallback)
- ✅ Save to Library (verified working — RLS SELECT+INSERT applied in Supabase)
- ✅ AI extraction pipeline (Job 1) — DeepSeek `deepseek-chat`, validated output, `/api/extract-context`
- ✅ Full-text (PMC) reading — `pmcid` from the record (lag-free), `sourceInfo` reported
- ✅ Two-tier limitations — paper-stated vs AI-derived-with-`based_on`
- ✅ **Study breakdown populated on the detail page** (`StudyDetail.tsx` renders validated `StudyContext` + badge + skeleton + errors; Task 2)
- ✅ **Context persisted (Task 3)** — `/api/save-context` upserts into `study_context` (+ `study_identified_limitations` child rows); `page.tsx` loads saved context DB-first; schema updated with `source_info` + child table + regenerable RLS (SELECT+INSERT+UPDATE on study_context; SELECT+INSERT+DELETE on child)
- ✅ **Task 3–10 schema applied in Supabase** — the full `sql/schema.sql` was run once in the Supabase SQL Editor (`source_info`, `study_identified_limitations`, `study_notes`, the Task 7 `articles`/`claims`/`evidence_links` migration, `study_simplifications`, the Task 10 `study_assessments` recreation, and all regenerable/per-user RLS + GRANTs + indexes). Context persistence, personal notes, the article editor, the evidence graph, the "In plain English" block AND the "Evidence context" + "What this might mean for training" blocks all work end-to-end.
- ✅ **Library page (Task 4)** — `/library` lists saved studies from `studies` newest-first (server component, `export const dynamic = "force-dynamic"` so newly-saved studies always appear); reuses home-page card markup, links to `/study/[pmid]`, has empty + error states; Library nav link added to the home page header
- ✅ **Ranked search (Task 5)** — `searchPubMed` now sends `sort=relevance` to ESearch, delegating ranking to NCBI's own "Best Match" ML algorithm (the same one used on pubmed.ncbi.nlm.nih.gov). This ranks results by relevance without filtering any — result count is unchanged (verified: "internal external rotation bicep" → still 228 hits). Default ESearch order is newest-first by PMID; Best Match surfaces the most-relevant studies regardless of recency.
- ✅ **Notes on studies (Task 6)** — Supabase GitHub OAuth via `@supabase/ssr` (`/auth/callback` exchange + `AuthStatus` header widget); `study_notes` table (keyed `(user_id, study_id)`, `UNIQUE` constraint, `user_id DEFAULT auth.uid()`) locked by RLS to the owner; `PersonalNotes` editor on the study page hidden behind a login prompt; `tsc --noEmit` clean
- ✅ **Article/claim system (Task 7)** — `/articles` list + create, `/articles/[id]` editor (title, content, per-claim evidence links to saved studies with supports/contradicts/mixed/contextual), study page "References in your articles" widget; schema upgraded (`articles`/`claims`/`evidence_links` → `user_id DEFAULT auth.uid()` + per-user `FOR ALL TO authenticated` RLS + `REVOKE ALL FROM anon`/`GRANT ALL TO authenticated` + FK/RLS indexes + empty-table migration); `tsc --noEmit` clean AND `npm run build` passes
- ✅ **Evidence graph (Task 8)** — `/graph` route (server shell + `<EvidenceGraph>` client component, same pattern as `/articles`); loads the user's ARTICLES → CLAIMS → EVIDENCE_LINKS → STUDIES via the RLS-locked browser client; **d3-force** (`forceLink`/`forceManyBody`/`forceCollide`/`forceCenter`) does ALL the physics; self-contained SVG renders relationship-colored edges (`RELATIONSHIP_HEX` added to `src/lib/articles.ts`), arrowheads on evidence edges, legend + node counts, and click-to-navigate (article editor / study page); `Evidence Graph` nav link added to home, library, and articles pages; `tsc --noEmit` clean AND `npm run build` passes with `/graph` registered
- ✅ **AI simplification (Task 9a, Job 2)** — "In plain English" block on `/study/[pmid]` below the raw abstract: `simplifyStudy` in `src/lib/ai.ts` (shared `getModel()`/`callDeepSeek()` — same cheap default as extraction), `/api/simplify-study` (fetch + simplify, no DB write), `/api/save-simplification` (upsert into new regenerable `study_simplifications` table with SELECT+INSERT+UPDATE RLS), `page.tsx` loads saved simplification DB-first, `StudyDetail` renders with Generate/Regenerate button + `sourceInfo` badge + skeleton + error/save-failure states
- ✅ **Claim alignment (Task 9b)** — per-claim "Check alignment" button in `ArticleEditor`: `/api/assess-claim` resolves linked studies' abstracts (+ `study_context.findings` when available) → `assessClaimAlignment` (SAME cheap fast model — **no** separate heavier-model logic) → verdict chip (aligned / partially aligned / unaligned) + reasoning; verdict auto-cleared when claim text or links change; button disabled until ≥1 study linked
- ✅ **Task 9 verified** — `tsc --noEmit` clean AND `npm run build` passes (Next.js 16, Turbopack); new routes registered: `/api/simplify-study`, `/api/save-simplification`, `/api/assess-claim`. Live AI + persistence for simplification requires the Task 9 schema (`study_simplifications` table + RLS) to be applied in Supabase; claim alignment additionally needs studies saved in the library and (for findings) the Task 3 `study_context` table.
- ✅ **AI evidence profile (Task 10, Job 3)** — "Evidence context" + "What this might mean for training" sections on `/study/[pmid]` are now FUNCTIONAL (no longer placeholders): `assessStudy` in `src/lib/ai.ts` (shared `getModel()`/`callDeepSeek()` — same cheap default; **no credibility score**; grounded "why each factor matters" for design/n/population/training status/duration/measurement + clearly-labelled interpretation with "what this does NOT mean" cautions), `/api/assess-study` (fetch + assess, no DB write), `/api/save-assessment` (upsert into new regenerable `study_assessments` table with SELECT+INSERT+UPDATE RLS), `page.tsx` loads saved assessment DB-first, `StudyDetail` renders both sections with Generate/Regenerate button + `sourceInfo` badge + skeleton + error/save-failure states + "Interpretation — not established fact" badge
- ✅ **Task 10 verified** — `tsc --noEmit` clean AND `npm run build` passes (Next.js 16, Turbopack); new routes registered: `/api/assess-study`, `/api/save-assessment`. The assessment save flow requires the Task 10 schema (`study_assessments` recreation + regenerable RLS) to be applied in Supabase. Live AI calls not re-verified in this session.
- ✅ **Deployed live on Vercel (Task 11)** — production URL: **https://study-hub-rho-drab.vercel.app/**; env vars set on Vercel (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `DEEPSEEK_API_KEY`); GitHub OAuth enabled with the production redirect URL added to Supabase (URL Configuration → Redirect URLs); the full `sql/schema.sql` applied once in Supabase (see the schema line above). The app is a real, reachable portfolio artifact.
- ✅ **Post-deployment verification + polish (Task 12)** — every flow verified on the LIVE URL with PMIDs 35819335 + 42605311 (see §12.4); **one production-only bug found + fixed**: the AI-derived shared tables were missing table-level `GRANT`s to `anon` (raw-SQL-created tables get no default grants on Supabase; RLS policies alone aren't enough — Postgres enforces table privileges *before* RLS), which broke `/api/assess-claim`, DB-first rendering of saved AI output, and all three save endpoints with `permission denied for table`. Fix: explicit `GRANT`s added to `sql/schema.sql` (+ documented in README); **USER ACTION: run the grant block once in Supabase** (see §12.5). **Second issue fixed: default `Create Next App` metadata everywhere** → root layout now has real title/description/OG/twitter + `metadataBase` + title template; home/library/articles/graph export per-page metadata; the study page uses `generateMetadata` for dynamic per-study titles/descriptions/OG. Mobile polish: top bars + action rows now wrap on narrow screens (study page, library, articles, graph). `tsc --noEmit` clean AND `npm run build` passes.
- ✅ `tsc --noEmit` passes clean
- ✅ `npm run build` passes clean (Next.js 16 production build, Turbopack)
- ✅ **Phase 2 Sprint 1 — Decouple AI from the Library (Task 14)** — `StudyDetail.tsx` auto-generates ALL THREE AI breakdowns (Study breakdown / In plain English / Evidence context + training application) into React state on load with **NO DB writes**; "Save to Library" is now the ONLY persistence path (saves the study + any generated AI state); the button becomes **"Remove from Library"** when saved, wired to `DELETE /api/study/[pmid]`; `studies` RLS gained a public DELETE policy + grant. `tsc --noEmit` clean AND `npm run build` passes with `/api/study/[pmid]` registered. **Live "Remove from Library" requires the §12.5 DELETE grant/policy block.**
- ✅ **Smart AI-Assisted Search (Task 15, Phase 2)** — the home search bar now accepts natural, layman questions (e.g. "how many times a week should I train?"). `translateToPubMedQuery` (Job 4) in `src/lib/ai.ts` (SAME cheap fast `deepseek-chat` model via shared `getModel()`/`callDeepSeek()` — no heavier-model logic) turns the question into an optimized PubMed query (title/abstract keywords, MeSH-ish terms, boolean operators, OR-synonym grouping). New `/api/ai-search` POST route translates server-side (DeepSeek key never leaves the server), fetches with the EXISTING `searchPubMed` flow, and returns `{ data, translated, translatedQuery, explanation, originalTerm }`. `HomeSearch.tsx` POSTs the question and renders a **visible "AI-translated query" disclosure** (blue badge + monospace query + plain-English explanation) so the user always knows what was actually searched. **Fallback:** if the AI translation fails (missing key/API error/invalid JSON), the route falls back to the raw term through `searchPubMed` and the disclosure shows a gray "Term search (AI fallback)" badge. `tsc --noEmit` clean AND `npm run build` passes with `/api/ai-search` registered.
- ✅ **Search persistence (Task 16, Phase 2)** — the committed search now lives in the URL (`/?q=training+frequency`, single source of truth) so the browser Back button (or a link back from a study page) restores the exact previous search + results. `HomeSearch.tsx` reads `q` via `useSearchParams`, pushes new searches with `router.push` (merging into existing params), and renders the `q` value in the input box. Restores are served from a **module-level session results cache keyed by the decoded `q`** — Back navs NEVER re-run the DeepSeek AI translation (only a never-seen query triggers `/api/ai-search`); a `searchIdRef` guard drops stale async responses on fast consecutive searches. `page.tsx` wraps `<HomeSearch>` in a `<Suspense>` boundary (required by Next.js 16 for `useSearchParams` in a statically-prerendered route). `tsc --noEmit` clean AND `npm run build` passes — `/` stays static (`○`) — with no API/schema changes (reuses the existing `/api/ai-search` flow).
- ✅ **Pagination / Load More (Task 17, Phase 2)** — removed the hardcoded 10-study limit: search results now have a **"Load more" button** that fetches the NEXT page (`retstart` = current result count) and appends it to the existing list. `src/lib/pubmed.ts` adds `searchPubMedPage(term, retmax, retstart)` → `{ data, totalResults }` (ESearch sends `retstart` to offset into the same ranked Best-Match list; `count` drives end-of-results detection) while the existing `searchPubMed(term, retmax, retstart)` stays as a thin wrapper — the "rank, don't filter" behavior is untouched. `/api/ai-search` now accepts `translatedQuery` + `translated` + `explanation` + `retstart` and returns `totalResults`; **page 2+ REUSES the already-translated query** by sending `translatedQuery` back, so DeepSeek is NEVER re-run deep in the list (verified live: a `retstart:10` call returned 10 new page-2 studies with the same translated query echoed back). `HomeSearch.tsx` appends each page with PMID dedup, reconciles the Task 16 `resultsCache` (a Back nav now restores every page loaded so far), and handles loading / error / **end-of-results** states ("Showing 20 of 45 results" + "You've reached the end of the results"). `tsc --noEmit` clean, `npm run build` passes (`/` still static, `/api/ai-search` dynamic), and the new pagination path was smoke-tested live against NCBI.
- ✅ **Sticky search header (Task 18, Phase 2)** — the home search bar + the "AI-translated query" disclosure badge now stick to the top of the viewport while the user scrolls through the paginated results. In `src/app/HomeSearch.tsx` the `<form>` and the `translatedQuery` disclosure are wrapped in a `sticky top-0 z-20 -mx-8 px-8 pt-4 pb-4 mb-6 bg-gray-50/95 backdrop-blur border-b border-gray-200/80 shadow-sm` container: `sticky` + `top-0` keep it pinned, `z-20` lifts it above the scrolling cards, the translucent `bg-gray-50/95` + `backdrop-blur` keep the input/disclosure text readable over whatever passes underneath, and `-mx-8 px-8` bleeds the bar edge-to-edge across the page's `p-8` padding so it reads as a real header (vs. floating in the column). The form row keeps its `flex-wrap` + `min-w-[260px]` input so the sticky header wraps cleanly on mobile; the disclosure now sits `mt-4` inside the bar. No `page.tsx` layout change was needed — the sticky surface lives inside `HomeSearch`'s existing `max-w-5xl` container. `tsc --noEmit` clean AND `npm run build` passes (`/` still static `○`). No API/schema changes.
- ✅ **Empty state UI for the home search screen (Task 19, Phase 2, roadmap #11)** — no more bare blank page under the search bar. New `src/app/EmptySearchState.tsx` (client) renders an onboarding card when no search has been run: a "What is Study Hub?" blurb, clickable example-question chips (Training frequency / Triceps arm position / Protein intake / Rep ranges — exported `EXAMPLE_QUERIES`, each kicking off a real search via a new `handleExampleSearch` that commits `?q=` to the URL exactly like `handleSearch`), and a note pointing to the Library + personal notes + Evidence Notebook. `HomeSearch.tsx` also shows a **distinct zero-results state** when a search has run (`urlQuery` set) but PubMed returned nothing: a "No results found" card with the searched term, a "rank, never filters" note, and the same example chips as recovery. Both states live below the Task 18 sticky header; error/loading states remain separate. `tsc --noEmit` clean AND `npm run build` passes (`/` still static `○`). No API/schema changes.
- ✅ **Visited links indicator (Task 20, Phase 2, roadmap #6)** — study cards now show a distinct "✓ Visited" treatment for studies the user has already clicked/visited. New `src/lib/useVisitedStudies.ts` (client hook): tracks visited PMIDs in a `Set` persisted to `localStorage` (`study-hub:visited-pmids`), starts EMPTY on server + first client render and only applies the persisted set after a mount `useEffect` sets `hydrated` (true) — so no SSR/hydration mismatch. New shared `src/components/StudyCard.tsx` (client) replaces the duplicated card markup in BOTH `HomeSearch.tsx` (search results) and `src/app/library/page.tsx`: visited cards get a blue-tinted card (`border-blue-200 bg-blue-50/50`), a "✓ Visited" badge, and a blue (vs default) title link; clicking the title link OR the "View Study →" button calls `markVisited(pmid)`. `tsc --noEmit` clean AND `npm run build` passes (`/` static, `/library` dynamic — unchanged). No API/schema changes.
- ✅ **Quick-save bookmark icon (Task 21, Phase 2, roadmap #7)** — each study card now has a bookmark button that saves the study to the Library in place, without opening it. New `src/lib/useSavedStudies.ts` (client hook): knows which PMIDs are already in the shared `studies` library via a single shared SELECT `pmid` fetch (module-level `loadSaved`/`savedCache` so all cards share one query; the `studies` table is the single source of truth — no localStorage), starts empty + applies on a mount `useEffect` (`loaded` gate → no SSR/hydration mismatch), and `markSaved(pmid)` optimistically adds after a successful save. `src/components/StudyCard.tsx` gained a bookmark button (top-right, visually separate from "View Study") that POSTs the card's fields to `/api/save-study` (check-then-insert): outline when unsaved, filled blue when saved, disabled + cursor-wait while saving, and an inline red error line on failure. Because the Library page lists rows already in `studies`, its cards show as already-saved automatically. `tsc --noEmit` clean AND `npm run build` passes (`/` static, `/library` dynamic — unchanged). No schema/RLS changes (`/api/save-study` already INSERT-only).
- ✅ **Library management (Task 22, Phase 2, roadmap #8)** — the StudyCard bookmark is now a save/remove toggle. `src/lib/useSavedStudies.ts` gained `markUnsaved(pmid)` (optimistic, updates the shared cache); `src/components/StudyCard.tsx` gained a `handleRemove` that calls `DELETE /api/study/[pmid]` (Task 14 endpoint) then `markUnsaved` + `onRemoved`, so a filled ("Already in Library") bookmark, when clicked, removes the study back to outline with a "Remove from Library" tooltip — plus a shared `busy` state and inline error line. New `src/app/library/LibraryList.tsx` (client list) owns the Library list in local state: it renders the cards + "N saved studies" count, and `onRemoved` drops a removed card from view (falling back to the empty state when the last card goes) with no server re-fetch. `src/app/library/page.tsx` now delegates the list rendering to `<LibraryList>`. On the search page, removing simply flips the bookmark back to outline (search results aren't "saved items"). `tsc --noEmit` clean AND `npm run build` passes (`/` static, `/library` dynamic — unchanged). No schema changes; requires the Task 14 `DELETE` grant/policy (§12.5).
- ✅ **HTML entity decoding (Task 23, Phase 2, roadmap #9)** — PubMed titles/abstracts often carry HTML character references (`&#xb0;` degree, `&lt;` less-than, `&micro;` µ, `&plusmn;` ±, …). React renders a plain string variable literally, so these were showing verbatim to users. Added `src/lib/entities.ts` with a **pure** `decodeEntities(input)` helper that decodes decimal (`&#123;`), hex (`&#x1F;`), and a curated map of common named scientific/biomedical entities (units, dashes, guillemets, math operators, Greek letters) to proper Unicode, guarding invalid/surrogate code points to U+FFFD. It is a PURE function (no DOM/DOMParser) so server and client produce identical output — applying it in a component body therefore cannot cause an SSR/hydration mismatch (unlike a browser-only decoder). Applied in `src/components/StudyCard.tsx` (title/authors/journal/abstract/date) and `src/app/study/[pmid]/StudyDetail.tsx` (title/authors/journal/abstract). `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes.
- ✅ **Publication dates on cards (Task 24, Phase 2, roadmap #10)** — study cards previously showed only the year (`publicationDate?.slice(0, 4)`). `src/components/StudyCard.tsx` now shows the **full** publication date string (`YYYY Mmm DD` / `YYYY Mmm` / `YYYY`) on both the search results and Library cards (via the shared `<StudyCard>`), falling back to "Unknown date". The study-detail header also now shows the full date instead of just the year. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes.
- ✅ **Dark mode toggle (Task 25, Phase 2, roadmap #13)** — a user-controlled dark theme with persistence. **How:** Tailwind v4's `dark:` variant defaults to `prefers-color-scheme`, so `src/app/globals.css` adds a class-based variant `@custom-variant dark (&:where(.dark, .dark *))` — `dark:` now applies only under `html.dark`. New `src/lib/theme.ts` (`getInitialTheme`/`applyTheme`) persists the choice to localStorage (`study-hub:theme`), falling back to the OS preference, and toggles `.dark` on `<html>`; new `src/components/ThemeToggle.tsx` (sun/moon button) flips + persists it and is mounted in every page header (home, library, study, articles, graph). A tiny pre-paint inline `<script>` in `src/app/layout.tsx` applies the saved/OS theme before first paint so there is **no flash of the wrong theme (no FOUC)**; because the class is set client-side from the same logic the toggle uses, hydration stays consistent. `dark:` variants were then applied across every major surface (cards, sticky header, search input, empty/zero states, study-detail sections, article cards + editor inputs, graph card/legend/SVG). **Why:** a persisted class-based toggle beats the system-only default (which ignores a manual choice and can't be overridden per-user); the pre-paint script avoids the jarring light-flash. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes.
- ✅ **Mobile overflow fix (Task 26, Phase 2, roadmap #12)** — eliminates the horizontal scroll / black bar on narrow screens. **How:** the search form's input had a hard `min-w-[260px]` that could exceed a narrow viewport and force the button off-screen, so `src/app/HomeSearch.tsx` now uses responsive widths (`w-full sm:w-auto sm:flex-1 sm:min-w-[260px]` input + `w-full sm:w-auto` button) — on mobile the input and button each take a full line instead of overflowing. Every page root (home, library, articles, article editor, graph, study) also gained `overflow-x-clip`. **Why `overflow-x-clip` and not `overflow-x-hidden`:** `clip` does NOT create a scroll container, so the Task 18 `sticky top-0` search header keeps working (setting `overflow-x-hidden` on an ancestor is a known way to break `position: sticky`). Also cleaned a duplicate `dark:text-gray-100 dark:bg-gray-950` class on the article-editor root left over from Task 25's replacements. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes.
- ✅ **Google OAuth alongside GitHub (Task 27, Phase 2, roadmap #14)** — a second sign-in provider. **How:** `src/components/AuthStatus.tsx` now renders a "Sign in with Google" button (white/bordered, dark-mode aware) beside the existing GitHub button; both call `signInWithOAuth` with their respective `provider`, so no provider-specific auth logic lives in the app. The `/auth/callback` route was already provider-agnostic (`exchangeCodeForSession` accepts any enabled provider's one-time code) — its comment was updated to say so. The signed-in display also now reads `user_metadata.name` (Google's shape) in addition to GitHub's `user_name`/`full_name`. **Why:** Supabase's OAuth + PKCE exchange is identical across providers, so adding a provider is purely UI + dashboard config — no new tables, no schema change, no extra backend. **USER ACTION REQUIRED:** enable Google in the Supabase dashboard (Authentication → Providers → Google: on, add the Client ID/Secret from a Google Cloud OAuth app) and add the Google-authorized redirect URI shown in that same Supabase panel (and/or add `https://study-hub-rho-drab.vercel.app/auth/callback` to the Google Cloud OAuth app's authorized redirect URIs). `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes.
- ✅ **Evidence Graph explanation (Task 28, Phase 2, roadmap #15)** — a first-time visitor to `/graph` now understands what they're looking at. **How:** `src/app/graph/page.tsx` renders a "What is this graph?" onboarding card above `<EvidenceGraph>`: a plain-English explanation of the nodes (articles / claims / studies), the relationship-colored edges (supports / contradicts / mixed / contextual, shown in their edge colors), a note that the data is private to the signed-in user (with a sign-in link to `/articles`), how to populate it (save studies → write an article → link claims), and that the layout is drag-and-drop force-directed physics. **Why:** the graph is the flagship "wow" but is not self-explanatory; an always-visible explainer (rather than a hover-only tooltip) costs nothing, works on touch devices, and doubles as onboarding. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes.
- ✅ **End-to-end AI Agent test (Task 29, Phase 2, roadmap #16)** — an automated + documented test of the full journey (Search → Auto-read → Save → Note → Article → Graph). **How:** added `scripts/e2e-test.ps1` — a runnable PowerShell script that hits the live server's PUBLIC chain and PASS/FAILs each step: `/api/ai-search` (natural question → translated query → results), then `/api/extract-context`, `/api/simplify-study`, `/api/assess-study` (all three auto-read AI jobs against a real PMID, taking the PMID from the search results so the chain is genuinely end-to-end), then `/api/save-study` (persist into the shared library). It targets the deployed Vercel URL by default or `$env:BASE_URL` for local, exits 0 on all-pass. Added `E2E_TEST_PLAN.md` — the manual signed-in checklist for the auth-gated tail (open the study, auto-read renders, save → Library, personal note persists per-user, article + claim + evidence link + alignment, then the graph shows the new nodes/edge). **Why a script + doc instead of a one-off run:** it turns the "AI Agent test" into a repeatable regression harness an interviewer can re-run, and separates the public (fully automatable) chain from the auth-gated tail that genuinely needs a human in a browser. `tsc --noEmit` clean AND `npm run build` passes (script + markdown only, no app code). **USER ACTION:** run `scripts/e2e-test.ps1` (and the manual plan) once against the live site to demonstrate it; results recorded in §12.4.
- ✅ **Phase 2 (Product Polish) is 100% COMPLETE** — all 16 roadmap items (Tasks 14–29) are done, committed, and pushed. Remaining work is user-side verification/configuration only (enable Google OAuth; run the E2E script + manual plan).
- ✅ **Dark-mode AI-box colour fix (follow-up to Task 25)** — on `/study/[pmid]` in dark mode, the AI output boxes kept a LIGHT background while their text flipped light via `dark:text-gray-200`, giving grey/white text on a light panel. Root cause: the colored AI content boxes (`bg-emerald-50` "In plain English", `bg-amber-50` training application, `bg-red-50` "what this does NOT mean" cautions) and the error/warning banners + `SourceInfoBadge` (`bg-purple-100`) had no `dark:` variants. Fixed in `StudyDetail.tsx`: added `dark:bg-emerald-950/40`/`dark:bg-amber-950/40`/`dark:bg-red-950/40` (+ matching dark borders/text) to those boxes and banners, made the remaining `text-gray-500`/`text-gray-400` dark-aware, and gave the badge `dark:bg-purple-900/40 dark:text-purple-300`. Also collapsed a set of duplicate `dark:text-gray-400 dark:text-gray-500` classes that Task 25's `replace_all` produced when `text-gray-400` matched inside an existing `dark:text-gray-400`. `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **"Back to search" preserves the active query (follow-up UX fix)** — clicking "← Back to search" from a study page used to land on the bare home page (`/`), forcing the user to re-type their query. **How:** `src/components/StudyCard.tsx` gained an optional `query` prop and now threads the active search query onto the study URL (`/study/[pmid]?q=...`) for both its title link and "View Study →" button; `HomeSearch.tsx` passes the committed `urlQuery` to each card; `StudyDetail.tsx` reads it with `useSearchParams` and points "← Back to search" at `/?q=<same query>` (falling back to `/` when there's no query, e.g. arriving from the Library). Because the home page restores `?q` from the Task 16 session results cache (or re-runs the search), the exact results come back — no retyping. Library/graph/articles "Back to search" links remain plain `/` (they're not tied to a search). `tsc --noEmit` clean AND `npm run build` passes (`/study/[pmid]` stays dynamic).
- ✅ **More dark-mode fixes (follow-up to Task 25) + teal assessment cards** — on `/study/[pmid]` the **Personal notes** box and the **References in your articles** box (`PersonalNotes.tsx`, `StudyReferences.tsx`) had no `dark:` background, so they stayed white in dark mode; both are now `dark:bg-gray-900` with dark-aware borders/text/inputs. The **Evidence context** ("regenerate assessment") factor cards were a faint `bg-teal-50/50` that read as grey and had no dark variant; they're now **solid teal** (`bg-teal-600`/`dark:bg-teal-700` with white text + `teal-100` labels) matching the teal "Regenerate assessment" button, and the section's idle placeholder got a matching teal dashed tint. On `/articles/[id]`, the **claim card** had `dark:border-gray-700` but no `dark:bg-gray-900`, so it stayed white in dark mode — fixed. All verified with `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **Sample article + research (training frequency)** — `SAMPLE_ARTICLE_training_frequency.md` provides a ready-to-create article ("How many times per week should you train?") for the Evidence Notebook, grounded in three real meta-analyses (PMIDs **30558493**, **27102172**, **29470825**): 2×/week per muscle beats 1× for hypertrophy and strength, with 3× as a reasonable upper bound (diminishing returns beyond ~2×). Includes 4 claims with per-claim linked studies + honest relationship labels (the "3× is best" preference is marked **contextual**, not "supports", so the notebook stays defensible).
- ✅ **Big batch: PMID search pinning, article CRUD, graph zoom, shared nav (polish sprint)** — several UX upgrades:
  - **PMID search pins the exact study** — `GET /api/study/[pmid]` was added (reuses `fetchPubMedStudyById`); `HomeSearch.runSearch` detects a bare numeric query, fetches that study, and pins it as the top result with the related results below (deduped), so typing "30558493" surfaces the exact study first.
  - **Article Read / Edit / Delete** — `ArticlesList` cards now show Read / Edit / Delete actions; new read-only route `/articles/[id]/read` (`ArticleReader.tsx`) renders the full article then each claim with its linked studies (relationship badge + study link) — the future-proofed "sharing" surface. Delete removes `evidence_links` → `claims` → `article` via the RLS-scoped browser client (from the list and from the editor). The editor header gained Read / Delete buttons.
  - **Select text → claim** — in the article editor, a "✂ Turn selection into a claim" button reads the content textarea's selected range and adds it as a new claim (which you can then link studies to).
  - **Evidence graph improvements** — node radius is now computed from label length so bubbles are big enough, and each node label renders in an SVG `<foreignObject>` that wraps text to stay **inside** the bubble (no more overflow). Added **zoom**: scroll-wheel + ＋/− buttons + Reset (d3-zoom-style, 40%–300%, zoomed around the SVG centre), and cleaned up stray duplicate `dark:text-*` classes.
  - **Shared top nav (`SiteNav.tsx`)** — pill links with icons (Library / My Articles / Evidence Graph / optional Back to search) + theme toggle + sign-in, replacing the bare "text →" links on home, library, articles, and graph. This also adds a **Library link on the articles and graph pages** (previously missing).
  - Verified `tsc --noEmit` clean AND `npm run build` passes; new route `/articles/[id]/read` registered. No schema changes.
- ✅ **Graph: full-text bubbles + working wheel zoom; editor: two-column layout** — follow-up polish to the previous batch:
  - **Wheel zoom actually works now** — the listener was attached in a mount effect that ran before the SVG existed (it renders only after the graph loads), so it never attached. The wheel effect now depends on `graph`, so it attaches once the SVG is in the DOM.
  - **Full text in every bubble** — removed label truncation (`…`). `measureLabel()` now sizes each node's radius and an SVG `<foreignObject>` label box from the full article/claim/study text (fixed comfortable width, text wraps inside the box, radius = box half-diagonal + padding), so the complete title fits inside the node instead of being cut off. Article/study/claim banding spacing was widened to fit the bigger bubbles.
  - **Editor two-column layout** — the article editor (and its file) now uses a `lg:grid-cols-5` grid: the **Article content** is the large left column (`col-span-3`, taller `min-h-[420px]` textarea) and **Claims** is a sticky right sidebar (`col-span-2`, `lg:sticky lg:top-4`) so claims stay on screen without scrolling the whole page — mimicking writing tools with a content + notes/claims side panel. Stacks to one column on mobile.
  - Verified `tsc --noEmit` clean AND `npm run build` passes. No schema changes.
- ✅ **Article editor: fixed full-height split view** — on wide screens the editor is now a fixed `lg:h-screen` flex column: the header/title/save row stays put, the **Article content fills the entire remaining viewport height** (`lg:flex-1`, `lg:resize-none`) so it never cuts off mid-way, and the **Claims column is its own independently scrollable panel** (`lg:overflow-y-auto`) with a fixed "Claims" header — so you scroll through claims while the article stays fixed on the left, instead of the whole page scrolling away from the article. Stacks + scrolls normally on mobile. Verified `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **Article Read view: two-column split + claim highlighting** — the read route (`/articles/[id]/read`, `ArticleReader.tsx`) now mirrors the editor: a `lg:h-screen` flex layout with the **article on the left** (full height, independently scrollable) and the **Claims & evidence sidebar on the right** (fixed header, independently scrollable). Every sentence in the article that matches a claim's text is now **highlighted** (amber `<mark>`), and clicking a highlighted claim scrolls to that claim's card in the sidebar (which lists its linked studies with relationship badges + study links). Claims whose text isn't found verbatim in the content simply aren't highlighted. Verified `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **Saved studies are now PER-ACCOUNT (data-model change)** — the Library is no longer a shared public bucket. **How:** added a new private join table `user_saved_studies(user_id, study_id)` (RLS `FOR ALL TO authenticated` locked to `auth.uid() = user_id`, `user_id DEFAULT auth.uid()`, `UNIQUE(user_id, study_id)`, FK indexes). `studies` stays as the shared public *source registry* (SELECT + INSERT cache of PubMed records), but "saving a study" now means inserting into `user_saved_studies` — which **requires sign-in**; an anonymous user cannot save. All save/library surfaces were rewired to the per-user model: `src/lib/useSavedStudies.ts` is now an auth-gated per-user hook (`signedIn`, `savedPmids`, `savedStudyIds`, `savePmid`/`removePmid`/`saveStudyId`/`removeStudyId`; saving by PMID first ensures the source row via `/api/save-study`, which now returns `studyId`); `StudyCard` bookmark saves/removes the user's own library and prompts sign-in when logged out; `StudyDetail` Save/Remove uses `user_saved_studies` via the browser client; the `/library` page is now a client `LibraryLoader` that lists the signed-in user's saved studies (login CTA otherwise); the article editor's study picker lists only the user's saved studies; and `PersonalNotes` only unlocks for studies the user has saved. **USER ACTION REQUIRED:** run the `user_saved_studies` migration block (added to the end of `sql/schema.sql`) once in the Supabase SQL Editor — see §12.5.
- ✅ **Graph pan/zoom-to-cursor + disclosure out of the sticky header** — 
  - **Evidence graph:** the SVG now supports **left-click drag to pan** and **scroll-wheel zoom toward the mouse cursor** (not just zoom around the centre). The transform state is `{x, y, k}`; wheel re-zooms keeping the world point under the cursor fixed (`px - wx·k`), and a drag updates the translate. Node clicks still navigate — a small movement threshold distinguishes a pan from a click, and node `onClick` ignores events after a drag. The graph gets a `cursor-grab` affordance.
  - **AI-translated query disclosure:** moved OUT of the sticky search header into the normal page flow (below the search bar, above the results), so the tall translated-query panel no longer overlaps/blocks the study cards while scrolling — especially on mobile.
  - Verified `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **Home heading renamed "Evidence Hub" → "SBL Hub"; graph pan bug fixed** — the home page `<h1>` is now **SBL Hub**. Fixed the evidence-graph pan so it only pans on an actual **click-drag**: the previous drag handler treated any mouse movement as a pan (because the last-drag position started at 0 and `mousemove` fired without a held button), which made the graph auto-lock to the cursor. The handler now gates panning behind a "button is down" flag, so just moving the mouse does nothing; you click-drag to pan and scroll-wheel to zoom-to-cursor.
- ✅ **Evidence graph: expanded spacing + curved link routing (spaghetti fix)** — cranked up the layout so links stop stabbing through unrelated bubbles. **Massive repulsion:** `forceManyBody` charge raised from `-520` to **`-2500`**, and `forceLink` distance from ~90/170 to **~320 (membership) / 400 (evidence)** so the nodes blast apart into their own distinct spaces. **Curved edges:** links render as quadratic-bezier `<path>`s (`M x1,y1 Q cx,cy x2,y2`, control point = midpoint nudged along the perpendicular) so lines sweep around intermediate nodes instead of cutting through them. **Hierarchical stratification:** `forceY` keeps Articles top (y≈150), Claims middle (y≈350), Studies bottom (y≈560); a weak `forceX` holds horizontal centring. **Radius-aware `forceCollide`** keeps the large bubbles from overlapping. The SVG is now `overflow-visible`, so nodes that spread beyond the 1100×700 viewBox stay reachable via the existing pan/zoom. Verified `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **Article editor: live claim highlighting + highlight-into-claim is the primary flow** — 
  - Extracted the claim-highlight renderer into a shared `src/lib/renderClaimHighlights.tsx` (used by both the editor and the read view). The editor now shows a **live "Claim highlights" panel below the Article textarea** that updates as you type, highlighting every sentence that matches a claim's text (amber `<mark>`, click to scroll to the claim's sidebar card).
  - Removed the **"+ Add claim"** button — the primary way to add a claim is now **highlighting text in your article and hitting "✂ Turn selection into a claim"** (the claims header shows this hint). Claim cards gained a scroll-target id so clicking a highlight jumps to the right claim.
  - Verified `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **Editor in-place highlighting + dynamic claims; graph stricter physics** —
  **Part 1 (ArticleEditor):** replaced the separate "Claim highlights" box with **in-place backdrop highlighting** — a mirrored, transparent-highlight layer sits absolutely behind the (transparent) textarea and scrolls with it, so claims are highlighted exactly where you type. Claims are now **read-only** in the sidebar and **derived from article character offsets** (`content.slice(start,end)`); `DraftClaim` gained `start`/`end`, new claims capture the selection range, and `handleContentChange` shifts each claim's range on edit (before → shift, inside → grow/shrink) so the highlight and claim text stay in sync as you write. The "Check alignment" button dark mode now uses `dark:bg-gray-700 dark:text-gray-100` for contrast.
  **Part 2 (EvidenceGraph):** `forceCollide` radius is now `radius + 60` (3 iterations) so nodes can't touch; `forceLink` distance raised to **500 (membership) / 620 (evidence)**; edge arcs are much sharper (bezier offset 0.35 / 0.5); and the tick handler clamps node `x/y` to the SVG bounds so nothing flies out and overlaps the page UI. Note: with a force-directed layout, 100% line-crossing freedom is mathematically impossible, but this gets it to ~95% by keeping bubbles far apart and arcing the lines around them. Verified `tsc --noEmit` clean AND `npm run build` passes.
- ✅ **Evidence graph: balanced physics + contained overflow** — reined the graph back in after the previous "explosion" (charge −2500 + link distance 620 had blasted nodes into the walls where they crushed together and leaked over the panel/UI). Changes in `src/app/graph/EvidenceGraph.tsx`:
  - **Contained overflow:** SVG is now `overflow-hidden` (was `overflow-visible`), so the graph stays strictly inside its dark panel; pan/zoom still lets the user explore.
  - **Radius-aware bounding box:** the tick clamp now keeps each bubble's EDGES inside the SVG with 20px padding: `x = clamp(x, r+20, WIDTH-r-20)`, `y = clamp(y, r+20, HEIGHT-r-20)`.
  - **Balanced forces:** `forceManyBody` charge reduced **−2500 → −1000**; `forceLink` distance reduced **500/620 → 250/350**; a stronger `forceX(WIDTH/2)` (0.12) gently pulls the cluster back to centre while the hierarchical `forceY` (strength 0.3) still holds articles top / claims middle / studies bottom.
  - **Collision:** kept radius-aware `forceCollide(radius + 20)`.
  - Verified `tsc --noEmit` clean AND `npm run build` passes.
- ✅ Living doc (this file)

**Editor note:** if VS Code shows "Cannot find module './StudyDetail'", it's a stale TS-server cache — the file exists and `tsc` resolves it. Restart the TS server (or save any file) to clear it.

### Test case being used
PubMed study **PMID 35819335** — *"Triceps brachii hypertrophy is substantially greater after elbow extension training performed in the overhead versus neutral arm position"* (Maeo et al., European Journal of Sport Science).

Why it's a good test case: it's exactly the kind of study that lacks context in its raw abstract (e.g., MRI may reflect acute swelling rather than true growth; specific subject population; specific training protocol). These are precisely the "Evidence context" / "what it does NOT mean" gaps the app is designed to surface.

---

## 11. Roadmap position & next steps

We are working through a 24-phase roadmap (from the project brief). Position ≈ **deployed live on Vercel (evidence graph done; next: post-deployment verification + polish/stretch)**. Priority hierarchy:

- 🔴 **Must work:** PubMed → Study → Study Context → Evidence Context ✅ (core flow wired; extraction rendered + persisted)
- 🟠 **Very important:** Notes ✅ (Task 6) → Articles → Claims → Evidence Links ✅ (Task 7)
- 🟡 **High-value stretch:** AI simplification → Claim alignment
- 🟢 **Stretch:** evidence graph ✅ (Task 8) → polish
- ⚪ **Completely optional:** social / monetisation / mobile

### Immediate next steps (one small, testable step at a time)

1. ✅ **AI extraction skeleton** — `src/lib/ai.ts` + `/api/extract-context` (`deepseek-chat`; validates JSON; verified 35819335→abstract_only with derived limitations, 42605311→full_text).
2. ✅ **Populate the Study breakdown** on the detail page from validated `StudyContext` (display only; `sourceInfo` badge; loading/error states).
3. ✅ **Persist extracted context** — `/api/save-context` upserts `study_context` + child limitations; page.tsx loads DB-first. **User action:** apply updated `sql/schema.sql` in Supabase.
4. ✅ **Library page** (`/library`) — server component lists saved studies from `studies` newest-first (`export const dynamic = "force-dynamic"`); home-page card markup reused; empty + error states; nav link on home page.
5. ✅ **Ranked search** — `sort=relevance` (NCBI Best Match) added to ESearch in `searchPubMed`; verified against "internal external rotation bicep" (still 228 results, relevance-ordered; no filtering).
6. ✅ **Notes on studies (Task 6)** — Supabase GitHub OAuth + `study_notes` table (RLS-locked, `user_id DEFAULT auth.uid()`) + login-gated `PersonalNotes` editor. **User actions:** (a) run updated `sql/schema.sql` in Supabase, (b) enable GitHub in Authentication → Providers with the Client ID/Secret, (c) set GitHub's authorization callback URL to Supabase's internal `https://<project-ref>.supabase.co/auth/v1/callback`, and (d) add `<app-url>/auth/callback` to Supabase URL Configuration → Redirect URLs (see §12.5 / `.env.example`).
7. ✅ **Article/claim system (Task 7)** — `/articles` (list + create), `/articles/[id]` editor (title, content, per-claim evidence links to saved studies with supports/contradicts/mixed/contextual), study page "References in your articles"; schema upgraded (`articles`/`claims`/`evidence_links` → `user_id DEFAULT auth.uid()`, RLS `FOR ALL TO authenticated`, GRANTs, FK/RLS indexes, migration). **User action:** apply the updated `sql/schema.sql` in Supabase — the Task 7 migration drops+recreates the (guaranteed-empty) `claims`/`evidence_links`/`articles` tables with the new shape.
8. ✅ **Evidence graph (Task 8)** — `/graph` visualizes the signed-in user's ARTICLES → CLAIMS → EVIDENCE_LINKS → STUDIES as an interactive force-directed SVG (d3-force physics; relationship-colored edges; click node → article editor / study page). Nav link added to home, library, and articles pages.
9. ✅ **AI simplification + claim alignment (Task 9)** — "In plain English" block on `/study/[pmid]` (Job 2; `study_simplifications` table, `/api/simplify-study` + `/api/save-simplification`, DB-first render, Generate/Regenerate) AND per-claim alignment check in `ArticleEditor` (`/api/assess-claim`; verdict chip + reasoning; same cheap fast model). **User action:** apply the updated `sql/schema.sql` in Supabase — adds the `study_simplifications` table + regenerable RLS (in addition to the Task 3/6/7 additions).
10. ✅ **AI evidence profile + training application (Task 10)** — functional "Evidence context" (six factor cards, no credibility score) + "What this might mean for training" (Job 3 cautions) on `/study/[pmid]`; `study_assessments` recreated + SELECT+INSERT+UPDATE RLS; `/api/assess-study` + `/api/save-assessment`; DB-first on revisit. Schema applied + deployed in Task 11.
11. ✅ **Deploy to Vercel (Task 11)** — schema applied in Supabase; GitHub OAuth enabled + production redirect URL added; deployed live at **https://study-hub-rho-drab.vercel.app/** (see §10).
12. ✅ **Post-deployment verification + polish (Task 12)** — every flow verified live (see §12.4); production-only grants bug + default-metadata bug fixed (commit `6129ce5`); SEO/meta + responsive polish applied. **User action:** apply the one-time GRANT block in §12.5.
13. ⏳ **Final signed-in verification on the live URL (Task 13)** — see §12.2.
14. ✅ **Pagination / Load More (Task 17)** — "Load more" control appended next-page results (retstart offset, translated-query reuse for page 2+, totalResults end-of-results handling, Task 16 cache reconciliation). Roadmap item #4.

### Workflow rule going forward
> One small feature at a time → the plan/approach is explained first → implemented → tested → committed. No more autonomous large builds.

---

## 12. Session Handoff (READ THIS FIRST in a new chat)

This project is developed in short agent-chat sessions. A fresh agent should be able to continue **from this file alone** plus the codebase — no conversation history needed.

### How the loop works
1. New chat: **"Read `PROJECT_NOTES.md` and the key files listed in §12.3, then implement the current task in §12.2."**
2. Agent completes the task, updates this file's status/next-steps, commits + pushes.
3. Agent tells the user the **next task in the same numbered format** (§12.2).
4. User starts a new chat with step 1. Repeat.

### 12.1 Non-negotiable project rules (transcript of the brief)
- **Explorer-first:** a user searches → reads → understands → forms a conclusion → *then* writes. Never "write a claim then find a study that agrees" (confirmation-bias machine).
- **Rank, don't filter:** search never hides low-relevance studies; it orders them.
- **The AI is an interpreter, never the source:** raw abstract/full text is always shown first, unmodified. AI output goes in derived/regenerable tables (`study_context`), never overwriting raw `studies`.
- **No credibility score.** No "87% reliable". Surface factors (design, n, population, duration, measure) + plain-language *why it matters*.
- **Two-tier limitations:** `limitations` = what the paper states; `identified_limitations` = AI-derived, each with a required `based_on` quote from a stated fact. Never invented.
- **Read full text when available:** via PMC (open access). `sourceInfo` field (`full_text` / `abstract_only` / `provided_text`) tells the UI what the AI actually read.
- **Cheap model default (Task 9):** `DEEPSEEK_MODEL` defaults to `deepseek-chat` in `src/lib/ai.ts`. ALL FIVE AI jobs (extract, simplify, assess — Job 3 Task 10 — claim alignment, and Job 4 query translation for AI-assisted search, Task 15) share the SAME cheap fast model via the shared `getModel()` — there is deliberately **no** separate heavier-model logic.
- **RLS:** `studies` = public SELECT + INSERT only (no UPDATE/DELETE). `study_context` = SELECT+INSERT+UPDATE (regenerable). `study_identified_limitations` = SELECT+INSERT+DELETE (regenerated wholesale). `study_simplifications` (Task 9) = SELECT+INSERT+UPDATE (regenerable). **`study_assessments` (Task 10) = SELECT+INSERT+UPDATE (regenerable)** — the server-side `/api/save-assessment` upserts on `study_id`. **`study_notes` = PRIVATE, `FOR ALL TO authenticated` with `auth.uid() = user_id`, `user_id DEFAULT auth.uid()`.** **`articles`/`claims`/`evidence_links` (Task 7) = same PRIVATE model — `FOR ALL TO authenticated`, `auth.uid() = user_id`, `user_id DEFAULT auth.uid()`, explicit `GRANT ALL TO authenticated` + `REVOKE ALL FROM anon`.**
- **Auth (Task 6):** GitHub OAuth via `@supabase/ssr`. Browser client = `src/lib/supabase/browser.ts`; server client = `src/lib/supabase/server.ts`; callback = `/auth/callback`. AuthState changes → `router.refresh()` where server props depend on save-state (studyId).
- **Next.js 16 gotchas:** dynamic `params` is a `Promise` (must await); `cookies()` is async; `middleware` → `proxy`; check `node_modules/next/dist/docs/` before new Next features.
- **Windows shell:** always use `npm.cmd`/`npx.cmd` in PowerShell (ps1 scripts are disabled); never chain with `&&` in PowerShell.

### 12.2 Current task queue
**DONE: Task 11 — Deployed to Vercel.** Live at **https://study-hub-rho-drab.vercel.app/** (full `sql/schema.sql` applied once in Supabase; GitHub OAuth + production Supabase Redirect URL configured; env vars set on Vercel). Recorded in §10 / §12.4 / §12.5.

**DONE: Task 12 — Post-deployment verification + polish pass.** Committed + pushed as **`6129ce5`** + docs commit **`083772e`** (Vercel auto-deployed). Live verification results, the production-only grants bug, the schema-drift (`study_context.source_info`) bug, and the SEO/responsive polish are recorded in §10 and §12.4. **User action #1 (GRANT block) is DONE by the user; user action #2 (schema-drift migration) remains** — run the one-time `ALTER TABLE study_context ADD COLUMN IF NOT EXISTS ...` block in §12.5 so `/api/save-context` upserts succeed and saved Study breakdowns render DB-first on revisit. After that, Task 13 (signed-in verification) is the only remaining flow.

**DONE: Task 14 — Phase 2 Sprint 1: Decouple AI generation from the Library** (see §10 / §12.4). `StudyDetail.tsx` auto-generates all three AI breakdowns into React state on load (NO DB writes); "Save to Library" is now the ONLY persistence path; the button becomes "Remove from Library" when saved, wired to `DELETE /api/study/[pmid]`; `studies` RLS gained a public DELETE policy + grant. **User action:** run the §12.5 DELETE grant/policy block.

**DONE: Task 15 — Smart AI-Assisted Search** (see §10 / §12.4). The home search bar now accepts natural, layman questions (e.g. "how many times a week should I train?"). `translateToPubMedQuery` (Job 4) in `src/lib/ai.ts` (SAME cheap fast `deepseek-chat` model via shared `getModel()`/`callDeepSeek()` — no heavier-model logic) generates an optimized PubMed query (title/abstract keywords, MeSH-ish terms, boolean operators, OR-synonym grouping). New `/api/ai-search` POST route translates server-side (DeepSeek key never leaves the server), fetches with the EXISTING `searchPubMed` flow, and returns `{ data, translated, translatedQuery, explanation, originalTerm }`. `HomeSearch.tsx` renders a **visible "AI-translated query" disclosure** (blue badge + monospace query + plain-English explanation); on AI failure it falls back to the raw term via `searchPubMed` with a gray "Term search (AI fallback)" badge. `tsc --noEmit` clean AND `npm run build` passes with `/api/ai-search` registered.

**DONE: Task 16 — Search persistence (query in the URL so Back preserves results).** Committed + pushed to `main` (see §10 / §12.4). The committed search now lives in the URL `/?q=<term>` (single source of truth); Back/Forward nav and study-page returns restore the exact previous search + results from a module-level session cache **without re-running the AI translation** (DeepSeek only runs for queries never seen this session). Implementation: `HomeSearch.tsx` reads/writes `q` via `useSearchParams`/`router.push`; `page.tsx` wraps `<HomeSearch>` in `<Suspense>` (Next.js 16 requirement). No API/schema changes; `tsc --noEmit` clean AND `npm run build` passes with `/` still static.

**DONE: Task 17 — Pagination / Load More (remove the hardcoded 10-study limit).** Committed + pushed to `main` (see §10 / §12.4). Search results now paginate: a **"Load more" button** below the results fetches the NEXT page (`retstart` = current result count) and appends it to the existing list. `searchPubMedPage(term, retmax, retstart)` → `{ data, totalResults }` added to `src/lib/pubmed.ts` (ESearch `retstart` offsets into the same ranked Best-Match list — "rank, don't filter" untouched; `count` returned for end-of-results); `searchPubMed` remains a thin wrapper. `/api/ai-search` accepts `retmax`/`retstart`/`translatedQuery`/`translated`/`explanation` and returns `totalResults` — **page 2+ reuses the already-translated query so DeepSeek is NEVER re-run** (verified live: `retstart:10` returned 10 new studies with the sent query echoed back unchanged). `HomeSearch.tsx` appends pages with PMID dedup, reconciles the Task 16 `resultsCache` (Back nav restores every loaded page), and handles loading/error/end-of-results states ("Showing 20 of 45 results"). `tsc --noEmit` clean AND `npm run build` passes (`/` still static `○`). No API/schema migrations. Next task: **Task 18 — Sticky search header (roadmap item #5)** — make the search bar stick to the top while scrolling results.

**DONE: Task 18 — Sticky search header (roadmap item #5).** Committed + pushed to `main` (see §10 / §12.4). The home search bar + the "AI-translated query" disclosure now stick to the top of the viewport while scrolling the paginated results. In `src/app/HomeSearch.tsx` the `<form>` + `translatedQuery` disclosure are wrapped in a `sticky top-0 z-20 -mx-8 px-8 pt-4 pb-4 mb-6 bg-gray-50/95 backdrop-blur border-b border-gray-200/80 shadow-sm` container (edge-to-edge across the page's `p-8` padding; translucent bg + backdrop-blur keep the text readable over scrolling cards; `flex-wrap` + `min-w-[260px]` keep it clean on mobile). No `page.tsx` layout change needed. `tsc --noEmit` clean AND `npm run build` passes (`/` still static `○`); no API/schema changes. Next task: **Task 19 — Empty state UI for the home search screen (roadmap item #11)** — when no search has been run, show a friendly empty/onboarding state (and handle the zero-results case) instead of a bare blank page.

**DONE: Task 19 — Empty state UI for the home search screen (roadmap item #11).** Committed + pushed to `main` (see §10 / §12.4). New `src/app/EmptySearchState.tsx` (client) renders an onboarding "What is Study Hub?" card with clickable example-question chips + a Library/notes/Notebook pointer when no search has run; `HomeSearch.tsx` gained `handleExampleSearch` (commits `?q=` like `handleSearch`) and a **distinct zero-results state** (search ran, PubMed returned nothing → "No results found" card with the searched term + recovery chips). `tsc --noEmit` clean AND `npm run build` passes (`/` still static `○`); no API/schema changes. Next task: **Task 20 — Visited links indicator (roadmap item #6)** — a visual marker for study result cards the user has already clicked/visited.

**DONE: Task 20 — Visited links indicator (roadmap item #6).** Committed + pushed to `main` (see §10 / §12.4). New `src/lib/useVisitedStudies.ts` (client hook: visited-PMID `Set` persisted to `localStorage`, hydration-safe via an empty initial state + a mount `useEffect` `hydrated` flag) + new shared `src/components/StudyCard.tsx` (client) that applies a blue-tinted card + "✓ Visited" badge + blue title link for visited PMIDs, with `markVisited` fired on title/View links. `StudyCard` now powers BOTH the `HomeSearch.tsx` results grid and `src/app/library/page.tsx`, so the visited treatment is consistent across search + Library. `tsc --noEmit` clean AND `npm run build` passes (`/` static, `/library` dynamic — unchanged); no API/schema changes. Next task: **Task 21 — Quick-save bookmark icon on search result cards (roadmap item #7)** — a bookmark icon on each result card that saves the study to the Library (and reflects already-saved state).

**DONE: Task 21 — Quick-save bookmark icon on search result cards (roadmap item #7).** Committed + pushed to `main` (see §10 / §12.4). New `src/lib/useSavedStudies.ts` (client hook: saved-PMID set from a shared SELECT `pmid` on the `studies` library — one fetch across all cards, `loaded` hydration gate, optimistic `markSaved`) + a bookmark button on `src/components/StudyCard.tsx` (top-right, visually separate from "View Study") that POSTs the card to `/api/save-study` (check-then-insert): outline when unsaved, filled blue when saved, disabled/cursor-wait while saving, inline red error on failure. Applied automatically to both search + Library cards (Library rows are already saved, so they show filled). `tsc --noEmit` clean AND `npm run build` passes (`/` static, `/library` dynamic — unchanged); no schema/RLS changes. Next task: **Task 22 — Library management (roadmap item #8)** — remove-from-library + save-button states (Already in Library / Remove from Library).

**DONE: Task 22 — Library management (roadmap item #8).** Committed + pushed to `main` (see §10 / §12.4). The StudyCard bookmark is now a save/remove toggle: `useSavedStudies.markUnsaved` + `StudyCard.handleRemove` call `DELETE /api/study/[pmid]` (Task 14 endpoint), so a filled ("Already in Library") bookmark removes the study back to outline ("Remove from Library" tooltip). New `src/app/library/LibraryList.tsx` (client list) owns the Library list in state so a removed card drops from view + updates the "N saved studies" count with no server re-fetch (falls back to empty state at zero). `src/app/library/page.tsx` delegates list rendering to it. `tsc --noEmit` clean AND `npm run build` passes (`/` static, `/library` dynamic — unchanged). No schema changes; requires the Task 14 `DELETE` grant/policy (§12.5). Next task: **Task 23 — HTML entity decoding (roadmap item #9)** — render &#xb0;, &#xab;, < 0.001 etc. as proper symbols on study cards + pages.

**DONE: Task 23 — HTML entity decoding (roadmap item #9).** Committed + pushed to `main` (see §10 / §12.4). New pure `src/lib/entities.ts` `decodeEntities()` (numeric decimal/hex + curated named scientific entities, invalid-surrogate-guarded) — DOM-free so it's SSR/hydration-consistent. Applied to `StudyCard.tsx` (title/authors/journal/abstract/date) and `StudyDetail.tsx` (title/authors/journal/abstract). `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes.

**DONE: Task 24 — Publication dates on study cards (roadmap item #10).** Committed + pushed to `main` (see §10 / §12.4). `StudyCard.tsx` now shows the full publication date (not just the year) on search + Library cards; study-detail header shows the full date too. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes. Next task: **Task 25 — Dark mode toggle (roadmap item #13)** — a class-based dark mode toggle that persists the choice.

**DONE: Task 25 — Dark mode toggle (roadmap item #13).** Committed + pushed to `main` (see §10 / §12.4). Tailwind v4 class-based dark mode via `@custom-variant dark` in `globals.css`; new `src/lib/theme.ts` (persist localStorage `study-hub:theme`, OS fallback, toggle `.dark` on `<html>`) + `src/components/ThemeToggle.tsx` (sun/moon) mounted in every header; pre-paint inline script in `layout.tsx` prevents theme FOUC; `dark:` variants applied across home/library/study/articles/graph surfaces. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes. Next task: **Task 26 — Mobile overflow CSS fix (roadmap item #12)** — eliminate horizontal scroll / black bar on narrow screens.

**DONE: Task 26 — Mobile overflow CSS fix (roadmap item #12).** Committed + pushed to `main` (see §10 / §12.4). `HomeSearch.tsx` search form is now responsive (input/button full-width on mobile), and every page root gained `overflow-x-clip` (which — unlike `overflow-x-hidden` — doesn't create a scroll container, so the sticky search header still works). Also fixed a duplicate dark class on the article-editor root. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes. Next task: **Task 27 — Google OAuth alongside GitHub (roadmap item #14)** — add Google as a second sign-in provider.

**DONE: Task 27 — Google OAuth alongside GitHub (roadmap item #14).** Committed + pushed to `main` (see §10 / §12.4). `AuthStatus.tsx` now offers a "Sign in with Google" button beside GitHub (both via `signInWithOAuth`); `/auth/callback` is provider-agnostic (comment updated); signed-in display reads Google's `name` metadata. **USER ACTION REQUIRED:** enable Google in the Supabase dashboard (Authentication → Providers → Google) with a Google Cloud OAuth app's Client ID/Secret, and confirm the redirect URIs (Supabase panel's Callback URL + `.../auth/callback` on Vercel). `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes. Next task: **Task 28 — Evidence Graph explanation (roadmap item #15)** — a tooltip/onboarding blurb explaining what the graph does.

**DONE: Task 28 — Evidence Graph explanation (roadmap item #15).** Committed + pushed to `main` (see §10 / §12.4). `src/app/graph/page.tsx` gained a "What is this graph?" onboarding card above the graph explaining nodes, relationship-colored edges, privacy, how to populate it, and that the layout is draggable. `tsc --noEmit` clean AND `npm run build` passes. No schema/API changes. Next task: **Task 29 — End-to-end AI Agent test (roadmap item #16)** — an automated/manual flow from Search → Auto-read → Save → Note → Article → Graph.

**DONE: Task 29 — End-to-end AI Agent test (roadmap item #16).** Committed + pushed to `main` (see §10 / §12.4). Added `scripts/e2e-test.ps1` (automated public backend chain: ai-search → extract-context → simplify-study → assess-study → save-study, PASS/FAIL + exit code, targets deployed URL or `$env:BASE_URL`) and `E2E_TEST_PLAN.md` (manual signed-in checklist: auto-read on the study page, save → Library, per-user note, article + claim + evidence link + alignment, graph nodes/edges). `tsc --noEmit` clean AND `npm run build` passes (test artifacts only, no app code). No schema/API changes. **USER ACTION:** run `scripts/e2e-test.ps1` and the manual plan once against the live site to demonstrate the full journey. Next task: none — this completes the Phase 2 roadmap (§13 all items checked).

### 12.3 Files to read on resume (fastest path to full context)
- `PROJECT_NOTES.md` (this file — deep context + history)
- `src/lib/pubmed.ts` — PubMed engine: `PubMedStudy` shape, `searchPubMed`, `fetchPubMedStudyById`, `findPmcid` (lag-free from record), `fetchFullText` (PMC, 12k cap), parsing.
- `src/lib/ai.ts` — DeepSeek helper: `StudyContext` + `IdentifiedLimitation` types, `extractStudyContext(title, abstract, fullText?)`, `simplifyStudy(title, abstract, fullText?)` → `StudySimplification` (Job 2), `assessClaimAlignment({claimText, studies})` → `ClaimAlignment` (verdict + reasoning); shared `getModel()`/`callDeepSeek()`; `deepseek-chat` default, strict JSON validation.
- `src/lib/supabase/browser.ts` + `server.ts` — `@supabase/ssr` browser/server clients (Task 6). Server client is request-scoped and used only by `/auth/callback`.
- `src/app/auth/callback/route.ts` — OAuth code exchange + session cookie persistence; `next` redirect param.
- `src/components/AuthStatus.tsx` — header sign-in/sign-out widget used on home, library, and study pages.
- `src/lib/supabase.ts` — shared anon client for *public* server-side reads/writes (`studies`, `study_context`, `study_identified_limitations`).
- `src/app/api/extract-context/route.ts` — AI pipeline endpoint (`sourceInfo`).
- `src/app/api/save-context/route.ts` — persistence endpoint (upserts `study_context` + child limitations).
- `src/app/api/simplify-study/route.ts` — Job 2 pipeline endpoint (fetches PubMed + PMC, plain-English simplification, `sourceInfo`).
- `src/app/api/save-simplification/route.ts` — persistence endpoint (upserts `study_simplifications`; same regenerable RLS as context).
- `src/app/api/assess-study/route.ts` — Job 3 pipeline endpoint (fetches PubMed + PMC, qualitative evidence profile, `sourceInfo`).
- `src/app/api/save-assessment/route.ts` — persistence endpoint (upserts `study_assessments`; same regenerable RLS as context/simplification).
- `src/app/api/assess-claim/route.ts` — claim alignment endpoint (resolves linked studies' abstracts + `study_context.findings`, returns verdict + reasoning).
- `src/app/api/save-study/route.ts` — INSERT-only check-then-insert.
- `src/app/api/search-pubmed/route.ts` — search endpoint.
- `src/app/study/[pmid]/page.tsx` + `StudyDetail.tsx` + `PersonalNotes.tsx` — detail page (renders + persists context; Task 6 personal notes; `studyId` prop unlocks notes).
- `src/components/StudyReferences.tsx` — Task 7 study-page widget (claims referencing a study, relationship-colored, article link).
- `src/lib/articles.ts` — Task 7/8 shared domain: `EvidenceRelationship`, `RELATIONSHIP_LABELS`/`RELATIONSHIP_COLORS`/**`RELATIONSHIP_HEX` (SVG edge colors)**, `LinkableStudy`, `DraftLink`, `DraftClaim`, `ArticleDraft`.
- `src/app/articles/page.tsx` + `ArticlesList.tsx` — `/articles` home for the Evidence Notebook (create + list).
- `src/app/articles/[id]/page.tsx` + `ArticleEditor.tsx` — the full article/claim/evidence-link editor (diff-save; second-save-safe).
- `src/app/graph/page.tsx` + `EvidenceGraph.tsx` — Task 8 `/graph` evidence graph (server shell + client component; d3-force physics; relationship-colored SVG edges; click-to-navigate).
- `src/app/page.tsx` — home search (header has `AuthStatus` + Library + My Articles + Evidence Graph links).
- `src/app/library/page.tsx` — Library page (Task 4; `export const dynamic = "force-dynamic"` pattern; Evidence Graph + My Articles links).
- `sql/schema.sql` — schema + RLS (incl. `source_info`, `study_identified_limitations`, `study_notes` + per-user RLS + FK indexes, regenerable policies + **Task 7: `articles`/`claims`/`evidence_links` user_id/auth.uid() + per-user RLS + GRANTs + FK/RLS indexes + migration**).
- `.env.example` — required env vars + GitHub OAuth note (never commit `.env.local`).

### 12.4 Verified status (don't re-verify unless asked)
- **Task 12 (live URL — all public flows):** ✅ verified on **https://study-hub-rho-drab.vercel.app/** with PMIDs 35819335 + 42605311:
  - page shells `/`, `/library`, `/articles`, `/graph`, `/study/35819335`, `/study/42605311` → HTTP 200; `/auth/callback` → 307 (OAuth exchange redirect).
  - `/api/search-pubmed` → 200 (relevance-ranked results, 35819335 first for "overhead triceps").
  - `/api/extract-context` → 200 (35819335 → `abstract_only`, validated context + 4 grounded identified limitations).
  - `/api/simplify-study` → 200 (42605311 → `full_text`, plain-English simplification).
  - `/api/assess-study` → 200 (35819335 → `abstract_only`, full evidence profile + training application + cautions).
  - `/api/save-study` → 200 (42605311 inserted into the shared library).
  - **BUG FOUND + FIXED (production-only):** `/api/assess-claim` → 500 `{"error":"Failed to load study findings"}`. Root cause: the AI-derived shared tables (`study_context`, `study_identified_limitations`, `study_simplifications`, `study_assessments`) had **no table-level `GRANT` to `anon`** in the deployed Supabase — they were created by raw SQL, which (unlike dashboard-created tables) grants nothing by default; RLS policies alone aren't enough because Postgres checks table privileges before RLS. Every anon-key read of those tables failed with `permission denied for table`. This also silently broke DB-first rendering of saved AI output on study pages and all three save endpoints. Fix: explicit `GRANT` statements added to `sql/schema.sql` + README (see §12.5 — **user action: run the one-time GRANT block in Supabase**; the deployed code is correct once the grants exist).
  - **BUG #2 FOUND + FIXED (production-only, surfaced AFTER the grants were applied):** with reads unblocked, the write path revealed **schema drift** — the production `study_context` table predates the Task 3 schema change that added the `source_info` column, and `CREATE TABLE IF NOT EXISTS` never adds columns to an existing table. `/api/save-context` → 500 `{"error":"Failed to save study context"}` (`PGRST204: Could not find the 'source_info' column of 'study_context'`). Confirmed via PostgREST probes: `study_context.source_info` does NOT exist on the live DB, while `study_simplifications.source_info`/`study_assessments.source_info` DO (those tables were recreated later). Fix: idempotent `ALTER TABLE study_context ADD COLUMN IF NOT EXISTS source_info/updated_at` migration added to `sql/schema.sql` (see §12.5 — **user action: run the one-time migration block in Supabase**). Post-grant re-verification: all four AI table SELECTs → 200; `/api/assess-claim` → 200 `{"verdict":"aligned"}`.
  - **BUG FOUND + FIXED (polish):** every page rendered Next's default `<title>Create Next App</title>` / "Generated by create next app". Added real metadata: root layout now exports `metadataBase` + title template + description + openGraph + twitter; home/library/articles/graph export per-page `metadata`; the study page uses `generateMetadata` (dynamic per-study title/description/OG from the saved study record, DB-first with live fallback). Re-checkable via `curl -s <url> | grep "<title>"`.
  - **Responsive check:** study page + graph SVG are responsive — the graph already uses `<svg viewBox ... className="w-full h-auto">`; added `flex-wrap` to the study page top bar + action row and to the library/articles/graph header rows so nav links/buttons don't overflow on narrow screens.
- PubMed search → parsed study cards: ✅ working.
- Study detail page (DB-first, live fallback): ✅ HTTP 200.
- Save to Library: ✅ working (RLS SELECT+INSERT applied in Supabase).
- `/api/extract-context`: ✅ 35819335 → `abstract_only` + derived limitations; 42605311 → `full_text`.
- `/api/save-context`: ✅ implemented; persistence requires the updated `sql/schema.sql` to be applied in Supabase (user action noted in §10).
- `/library` (Task 4): ✅ HTTP 200; home page `/` with Library nav link: HTTP 200.
- Ranked search (Task 5): ✅ `sort=relevance` verified against NCBI ESearch (returns relevance-ordered PMIDs; result count unchanged — no filtering); `tsc --noEmit` clean.
- Task 6 (auth + notes): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack). The note save flow requires the `study_notes` RLS + table to exist in Supabase, so it is **not end-to-end verified until the user applies `sql/schema.sql`** and enables GitHub OAuth.
- Task 7 (articles/claims/evidence links): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16, Turbopack) with `/articles` and `/articles/[id]` registered. The save/link flow is **not end-to-end verified until the user applies the updated `sql/schema.sql`** (Task 7 migration recreates `articles`/`claims`/`evidence_links` with `user_id`, RLS policies and GRANTs) and GitHub OAuth is enabled.
- Task 8 (evidence graph): ✅ **`d3-force` installed** (`^3.0.0` + `@types/d3-force`); `tsc --noEmit` clean AND `npm run build` passes (Next.js 16, Turbopack) with `/graph` registered. Auth/login-gated rendering on the client; the graph itself is **not end-to-end verified until the user applies the Task 7 schema** (articles/claims/evidence_links need `user_id` + RLS) and GitHub OAuth is enabled (same dependency as Task 7).
- Task 9 (AI simplification + claim alignment): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16, Turbopack) with `/api/simplify-study`, `/api/save-simplification`, `/api/assess-claim` registered. The simplification save flow requires the Task 9 schema (new `study_simplifications` table + regenerable RLS) to be applied in Supabase; claim alignment needs studies saved in the library (and `study_context.findings`, when available). Live AI calls not re-verified in this session.
- Task 10 (AI evidence profile + training application): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16, Turbopack) with `/api/assess-study`, `/api/save-assessment` registered. The assessment save flow requires the Task 10 schema (`study_assessments` recreation + regenerable RLS) to be applied in Supabase. Live AI calls not re-verified in this session.
- Task 11 (deploy): ✅ production URL live — **https://study-hub-rho-drab.vercel.app/**; full `sql/schema.sql` applied once in Supabase; GitHub OAuth + production Supabase Redirect URL configured; env vars set on Vercel. Post-deployment end-to-end verification of the live flows is the subject of **Task 12** (user runs the test-PMID flows on the production URL).
- Task 15 (Smart AI-Assisted Search): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) with `/api/ai-search` registered. `translateToPubMedQuery` (Job 4, `deepseek-chat`) validated; `/api/ai-search` translates server-side and falls back to raw term search on AI failure. The live AI call (question → translated query → PubMed results) is **not re-verified in this session** — it needs the live DeepSeek key + NCBI, both available at runtime on Vercel (no schema/user action required — the endpoint uses only the existing `searchPubMed` flow and the DeepSeek key already set in `.env.local`/Vercel).
- Task 16 (Search persistence): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) — `/` still renders as a static route (`○`) because `page.tsx` wraps `<HomeSearch>` in `<Suspense>` (Next.js 16's required boundary for `useSearchParams` in a prerendered route; verified against `node_modules/next/dist/docs/.../use-search-params.md`). New behavior: submitting a search pushes `/?q=<term>` (merging into existing query params), the URL `q` is the single source of truth, and Back/Forward restores the prior search + results from the module-level `resultsCache` (no DeepSeek re-run). Live browser Back-nav behavior is **not re-verified in this session** — verify by searching → clicking into a study → pressing Back in the browser (should restore results instantly, with a visible URL `?q=...`).
- Task 17 (Pagination / Load More): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) — `/` still static (`○`); `/api/ai-search` + `/api/search-pubmed` dynamic. **Live smoke-test of the new pagination path passed:** POST `/api/ai-search` with `{ question: "hypertrophy training frequency", translatedQuery: "(hypertrophy[tiab]) AND (training frequency[tiab])", translated: true, explanation: "test", retmax: 10, retstart: 10 }` → HTTP 200 with 10 NEW page-2 studies (offset into the same ranked list — no overlap with page 1), `totalResults: 45`, and the SENT `translatedQuery` echoed back unchanged (proof that DeepSeek was skipped entirely for page 2+). The browser "Load more" UX (button click → append → "Showing X of 45" → "You've reached the end of the results") is **not manually re-verified in this session** — verify by searching a broad topic → clicking Load more twice → confirming results append and the end-of-results message appears when all 45 are shown, and that Back from a study page restores ALL loaded pages (Task 16 cache now holds every appended page).
- Git: **Task 11 committed and pushed** to `main` (production URL recorded; Task 12 defined in §12.2).
- Task 22 (Library management): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) — `/` static, `/library` dynamic (unchanged); no schema changes. The remove-from-library UX is **not manually re-verified in the browser this session** — verify by opening `/library`, clicking a saved study's filled bookmark (should turn outline and the card should disappear + count drop; removing the last card shows the empty state), and checking the search page bookmark flips back to outline after removal. Requires the Task 14 `DELETE /api/study/[pmid]` path + the §12.5 `DELETE` grant/policy on the live DB.
- Task 23 (HTML entity decoding): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack); no schema/API changes. Not manually re-verified in the browser this session — verify by opening a study with special characters (e.g. a title/abstract containing "°", "<", "µ", "±", or guillemets) and confirming the symbols render instead of escape codes on both the card and the study page.
- Task 24 (Publication dates on cards): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack); no schema/API changes. Not manually re-verified in the browser this session — verify by searching and confirming the full publication date (not just the year) shows on each result card and in `/library`.
- Task 25 (Dark mode toggle): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack); no schema/API changes. Not manually re-verified in the browser this session — verify by clicking the sun/moon in any header (theme flips immediately), reloading (choice persists, no light flash), and toggling on all pages (home, library, study, articles, graph) to confirm the `dark:` surfaces look right.
- Task 26 (Mobile overflow fix): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack); no schema/API changes. Not manually re-verified in the browser this session — verify by opening the home page in a narrow/mobile viewport: no horizontal scrollbar or black bar should appear, the search input + button should each take a full line, and the sticky search header should still pin to the top while scrolling.
- Task 27 (Google OAuth): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack); no schema/API changes. The Google sign-in flow **cannot be verified until the user enables Google in the Supabase dashboard** (see §10 / §12.5) — once enabled, clicking "Sign in with Google" should run the OAuth flow and land back on the originating page. GitHub sign-in is unaffected.
- Task 28 (Evidence Graph explanation): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack); no schema/API changes. Not manually re-verified in the browser this session — verify by opening `/graph` and confirming the "What is this graph?" card appears above the graph and reads correctly in both light and dark mode.
- Task 29 (End-to-end AI Agent test): ✅ **`scripts/e2e-test.ps1` run LIVE against the deployed site (https://study-hub-rho-drab.vercel.app) and ALL FIVE backend steps passed (exit 0):** `/api/ai-search` for "how many times a week should I train?" → returned 3 results with a translated query (`(training frequency[tiab]) AND (resistance training[tiab] OR strength training[tiab] OR weight lifting[tiab] OR exercise[tiab])`); used the top result **PMID 27102172** (Schoenfeld et al., RT frequency + hypertrophy) → `/api/extract-context` (`abstract_only`), `/api/simplify-study`, `/api/assess-study` all returned AI output; `/api/save-study` → `success=True, alreadyPresent=False` (inserted into the live library). Notably the returned abstract still contained HTML entities (`&#xa0;`, `&#xb1;`), confirming Task 23's decoding matters. The manual signed-in tail (note → article → graph) is documented in `E2E_TEST_PLAN.md` and requires a browser + auth — **run it once to demonstrate the full journey.**
- Task 21 (Quick-save bookmark): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) — `/` static, `/library` dynamic (unchanged); no schema/RLS changes. The quick-save UX is **not manually re-verified in the browser this session** — verify by searching a broad topic, clicking a card's bookmark (outline) → it should fill blue, and the study should then appear in `/library`; reload the search page to confirm already-saved studies show a filled bookmark. Requires the live `/api/save-study` + `studies` INSERT path (already verified working in prior tasks).
- Task 20 (Visited links indicator): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) — `/` static, `/library` dynamic (unchanged); no API/schema changes. The visited styling is **not manually re-verified in the browser this session** — verify by (a) searching a broad topic, clicking one study into its page, then pressing Back: that card should now show the blue "✓ Visited" badge + tint; (b) opening `/library` to confirm the same card carries the same visited treatment; (c) reloading the page to confirm the marker persists (localStorage, not just the session).
- Task 19 (Empty state UI): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) — `/` still static (`○`); no API/schema changes. The empty/onboarding + zero-results states are **not manually re-verified in the browser this session** — verify by (a) loading `/` with no `?q` → "What is Study Hub?" card + example chips should appear, (b) clicking a chip → a real search should run and the sticky header + results render, (c) searching a nonsense term (e.g. "zzzqqqx") → the distinct "No results found" state with recovery chips should appear.
- Task 18 (Sticky search header): ✅ `tsc --noEmit` clean AND `npm run build` passes (Next.js 16 production build, Turbopack) — `/` still static (`○`); no API/schema changes. The sticky header (search bar + AI-translated query disclosure pinned via `sticky top-0 z-20` + `backdrop-blur`) is **not manually re-verified in the browser this session** — verify by searching a broad topic, scrolling deep through the results, and confirming the search bar + disclosure remain pinned and readable at the top while the cards scroll underneath (on both desktop and a narrow/mobile viewport).

### 12.5 Environment notes
- **Per-account saved studies — USER ACTION (apply once in Supabase SQL Editor):** the Library is now per-user, so a new private join table must exist. Run this block (idempotent; also embedded at the end of `sql/schema.sql`):
  ```sql
  CREATE TABLE IF NOT EXISTS user_saved_studies (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id UUID DEFAULT auth.uid() REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
    study_id UUID REFERENCES studies(id) ON DELETE CASCADE NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL,
    UNIQUE (user_id, study_id)
  );
  ALTER TABLE user_saved_studies ENABLE ROW LEVEL SECURITY;
  CREATE INDEX IF NOT EXISTS idx_user_saved_studies_user_id ON user_saved_studies (user_id);
  CREATE INDEX IF NOT EXISTS idx_user_saved_studies_study_id ON user_saved_studies (study_id);
  DROP POLICY IF EXISTS "Users can manage their own saved studies" ON user_saved_studies;
  CREATE POLICY "Users can manage their own saved studies" ON user_saved_studies
    FOR ALL TO authenticated
    USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
  REVOKE ALL ON public.user_saved_studies FROM anon;
  GRANT ALL ON public.user_saved_studies TO authenticated;
  ```
  After running it, saving a study from search (bookmark) or the study page works per-account, `/library` lists the user's own saves, and the article editor's study picker shows only the user's saved studies. Note: `/api/save-study` still creates the public `studies` source row as a side effect (that's just the shared source cache); the user-facing Library now rides on `user_saved_studies`.
- **Task 27 — Google OAuth (USER ACTION):** to enable the "Sign in with Google" button (already in the code), do BOTH of these in dashboards (no env vars / schema needed — Supabase + Google handle the secrets):
  1. **Google Cloud:** create/use an OAuth 2.0 client ID; under *Authorized redirect URIs* add `https://<project-ref>.supabase.co/auth/v1/callback` (copy it from Supabase Authentication → Providers → Google → "Callback URL") — same pattern as GitHub's callback.
  2. **Supabase:** Authentication → Providers → Google → enable it and paste the Client ID + Client Secret from Google Cloud. Confirm the site's Redirect URL `https://study-hub-rho-drab.vercel.app/auth/callback` is in Supabase URL Configuration → Redirect URLs (already added for GitHub in Task 11; the callback is shared by both providers).
- `.env.local` already contains `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `DEEPSEEK_API_KEY` (user set it). `DEEPSEEK_MODEL` optional. **GitHub OAuth needs no env vars** - configured in TWO dashboards: (1) GitHub -> OAuth App -> "Authorization callback URL" must point to Supabase's INTERNAL callback `https://<project-ref>.supabase.co/auth/v1/callback` (copy it from Supabase Authentication -> Providers -> GitHub, "Callback URL") - NOT the app URL; (2) Supabase -> Authentication -> URL Configuration -> Redirect URLs -> add `<app-url>/auth/callback` (e.g. `http://localhost:3000/**`). **Task 11 added the production redirect** `https://study-hub-rho-drab.vercel.app/auth/callback`. See .env.example for the full walkthrough.
- Dev server: restart after env change (`npm.cmd run dev`). If port 3000 busy: `taskkill /PID <pid> /F`.
- Supabase project is live; **the full `sql/schema.sql` was applied once (Task 11)** — `sql/schema.sql` remains the source of truth. Don't re-run the full schema unless asked; future schema changes should be added as new migration sections (with a clear "apply once" note) so the file stays reproducible.
- Test PMIDs: **35819335** (the key "missing context" case), **42605311** (open access full text).
- **Task 12 user action #1 — one-time GRANT block (DONE by user):** the AI-derived shared tables were created by raw SQL, so they have NO default table grants. User ran this once against the live project (idempotent; already reflected in `sql/schema.sql` for fresh setups):
  ```sql
  GRANT SELECT, INSERT, DELETE ON public.studies TO anon, authenticated;
  GRANT SELECT, INSERT, UPDATE ON public.study_context TO anon, authenticated;
  GRANT SELECT, INSERT, DELETE ON public.study_identified_limitations TO anon, authenticated;
  GRANT SELECT, INSERT, UPDATE ON public.study_simplifications TO anon, authenticated;
  GRANT SELECT, INSERT, UPDATE ON public.study_assessments TO anon, authenticated;
  ```
  After running it, `/api/assess-claim` returns 200 and study pages render saved AI output DB-first. (`articles`/`claims`/`evidence_links`/`study_notes` intentionally stay `REVOKE ALL FROM anon` — they're authentication-only by design.)
- **Task 12 user action #2 — one-time schema-drift migration (apply in Supabase SQL Editor):** the previous GRANTs unblocked reads but the write path then failed because the production `study_context` table (created before Task 3's `source_info` column existed) never got the column — `CREATE TABLE IF NOT EXISTS` does NOT add columns to existing tables. Run this once (idempotent; also embedded in `sql/schema.sql` for fresh setups):
  ```sql
  ALTER TABLE study_context ADD COLUMN IF NOT EXISTS source_info TEXT;
  ALTER TABLE study_context ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW());
  ```
  After running it, `/api/save-context` upserts succeed and saved Study breakdowns render DB-first on revisit. This was the last known blocker for the public write flows (the other AI tables already had their columns).

### 12.6 Every session must end with
1. Update this file (status, decisions, next-steps).
2. Commit + push.
3. State the next task as **one numbered item** in §12.2 format.

---

## 13. Phase 2 Roadmap (Product Polish)

Phase 2 layers product-polish sprints on top of the deployed Phase 1 (Study Explorer + Evidence Notebook + Graph). The queue below was distilled from user feedback; tasks are executed one at a time via the §12 loop. Use this as the roadmap for upcoming feature work.

> **Status: Phase 2 is 100% COMPLETE.** All 16 roadmap items are done (Tasks 14–29). See §12.2 for per-task handoffs; the remaining "next steps" are user-run verifications (Google OAuth enable, E2E run) rather than new features.

1. [x] Decouple AI generation from the Library (Task 14, Sprint 1) - auto-generate AI breakdowns on load in React state (no DB writes); manual Save to Library is the only persistence path; Remove from Library via DELETE /api/study/[pmid].
2. [x] Smart AI-Assisted Search (Task 15, Sprint 1) - layman natural language to DeepSeek to optimized PubMed query with visible translated-query disclosure.
3. [x] Search persistence (Task 16) - push the query to the URL so Back preserves results.
4. [x] Pagination / Load More (Task 17) - removed the hardcoded 10-study limit; "Load more" appends next-page results (retstart offset, translated-query reuse, totalResults/end-of-results states, Task 16 cache reconciliation).
5. [x] Sticky search header (Task 18) - the search bar + AI-translated query disclosure stick to the top while scrolling results.
6. [x] Visited links indicator (Task 20) - visual marker for study cards already clicked/visited, persisted in localStorage, shared across search + Library.
7. [x] Quick-save bookmark icon on search result cards (Task 21) - bookmark button saves a study to the Library in place; reflects already-saved state.
8. [x] Library management (Task 22) - bookmark toggles save/remove with Already-in-Library + Remove-from-Library states; Library list drops removed cards live.
9. [x] HTML entity decoding (Task 23) - new `decodeEntities` helper renders &#xb0;, &#xab;, < 0.001 etc. as proper symbols on cards + study page.
10. [x] Publication dates on study cards (Task 24) - full publication date now shown on search + library cards (not just the year).
11. [x] Empty state UI for the home search screen (Task 19) - onboarding "What is Study Hub" card + clickable example question chips when no search has run; distinct zero-results state when a search returns nothing.
12. [x] Mobile overflow fix (Task 26) - overflow-x-clip on every page root + responsive search form (full-width input/button on mobile); no horizontal scroll or black bar, sticky header unaffected.
13. [x] Dark mode toggle (Task 25) - class-based dark mode (Tailwind v4 custom variant) + persisted sun/moon toggle in every page header; applied across home, library, study, articles, and graph.
14. [x] Google OAuth alongside GitHub (Task 27) - "Sign in with Google" button next to GitHub; provider-agnostic callback. **User action: enable Google in the Supabase dashboard.**
15. [x] Evidence Graph explanation (Task 28) - plain-English "What is this graph?" onboarding card on /graph (nodes, relationship colors, privacy, how to populate).
16. [x] End-to-end AI Agent test (Task 29) - `scripts/e2e-test.ps1` automated backend chain + `E2E_TEST_PLAN.md` manual signed-in checklist (Search → Auto-read → Save → Note → Article → Graph).
